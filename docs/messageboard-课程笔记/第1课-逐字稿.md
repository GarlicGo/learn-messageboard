会议主题：1-后端培训项目 留言板 讲解（1）-480P 标清-AVC
上传时间：2026年3月31日（周二） 02:17 （GMT+08）
智能纪要：1-后端培训项目 留言板 讲解（1）-480P 标清-AVC 2026年3月31日

说话人 1 00:00:02
我们来新建一个项目，点击文件新建项目，这有很多项目类型，我们选的是这个 spring initializer， 名字就选 message board， 然后位置就放在 d 盘的根目录下，然后你们可以选你们自己喜欢的位置放语言选 Java， 类型选 Maven， 然后组选 club， 点 dev Hub， 这个是写公司域名的逆序，像社区的域名就是 dev Hub 点 club， 然后反过来写就是 club 点 dev Hub。工件和名字保持一致，软件包名称就是这俩拼起来。

说话人 1 00:00:36
JDK 选我是17，然后你们也可以选自己的版本。Java 我就选了17打包打成 jar 包，点击下一步，这个是我喜欢，不用最新版，我就选6.2.6.9我们勾一些需要用到的依赖，其实这个 springweb 你多勾少勾都无所谓，因为到时候都是可以到 POM 点 XML 里改的，你这些勾选的这些东西最终都是会写到 POM 点 XML 里面的模板引擎，我们用不到。然后 security 用的 spring security SQL 是用到了 Mybatis 还有 MySQL 点击创建。

说话人 1 00:01:29
它现在已经下载好了，我们在新窗口打开，我们看一下它这个项目结构，其实它有很多东西都用不到了，像这个 help 点儿。MD， 这是可以直接删的，我就把它删掉了。还有这些像 m v n w m v n w 点 CMD， 还有点 MVN， 这个 maven- wrapper 是用来给那些电脑上没有装 Maven 的人用的，但是我们 idea 它都自带了 Maven， 所以我们不用这个东西，可以直接删，我们再展开。

说话人 1 00:02:17
我们看一下这几个文件夹，这个 Java 文件夹就是放你的 Java 源代码的，然后 resource 文件夹就是放配置文件的，例如 XML 文件，或者是一些其他的像 XML 文件等等。然后这个 test 在底下，这个 Java 文件夹是放到测试类的，就是写单元测试，写在这里面的源代码的文件夹是用蓝色标，然后这个资源的是有一个这个图标就是资源，然后绿色的是放测试代码的，我们点开以后这是一个启动类。下面用 spring boot application 注解标注的这个类就是启动类，我们看一下 resource 文件夹里边这个 static 是放静态文件的，像图片什么的，但是我们现在是前后端分离的项目，所以这个 static 文件夹是不用的，可以直接删掉，像 templates 是放模板引擎的，我们现在只有前后端不分离才用得着 template 这个存模板引擎。我们现在是前端和后端分离，以后这个 template 文件夹也不用了。可以直接删，现在精简完以后就这样一个结构， POM 点儿 XML 是 Maven 的构建规范。然后 gitignore 就是写哪些东西不要上传到 Git 仓库上去， src 就是你的代码了， main Java 代码。

说话人 1 00:03:49
然后资源以及测试，我们从 POM 点 XML 开始讲，这个就是 Maven 的一个构建规范，这有一个 parent 说明它是继承了一个父项目，它 parent 写的是 spring boot starter parent， 然后版本是2.6.9。这也是刚才我们在 spring initializer 里边设置的 group ID， 也是我们刚才填的。然后 ARTIFACT ID 和 version 也是我们刚才在用 idea 那个图形化界面初始化的时候选择的这个 Java version 我选的是17，如果你们不是17的话就可以改低成你们自己的版本。

说话人 1 00:04:27
这个 dependency 就是依赖 Maven， 就是一个项目管理工具，可以对 Java 项目进行自动化的构建和依赖管理。我们可以在这个 dependencies 这里面添加一些自己要的 dependency。然后 Maven 就会自动帮我们去中央仓库里边下载这些 jar 包，然后我们就可以直接用，然后这有一个 plugins， 这个 plugins 就是插件，我们可以看一下 idea 的这个 Maven， 展开这个生命周期，对应的是 Maven 的一些基本操作，然后插件以及依赖项，我们看到这个依赖项是和我们这写的依赖项是一一对应的，像这个 spring boot STARTER security。这个也是拉过来看一下 security， 然后这个是 Web， 然后这是 Mybatis， 这也是 Mybatis 以及这个 Mysql Longbook， 然后 test， 然后 security test， 这也都对应的。不过这个 security test 用不到可以删掉。

说话人 1 00:05:36
你每次对 POM 点 XML 做完更改以后，都应该点一下这个加载 Maven 变更，加载完以后你会发现它这个依赖项就少了一个，因为你光改 POM 点 XML 却不点这个加载 Maven 项目的话，其实你做的这些更改它也不会生效的。我们再拿这个 long book 来举个例子，我先，我们先不注释掉它，我们看一下它现在它是有的依赖项， message 一步可以看到它这个项目是已经依赖这个 long book 的，我们把它注释掉后再点这个加载没有变更，我们现在可以看到 long book 就没有了，因为我们把它的依赖去除以后。我们就没有这个 jar 包了，我们也用不了 Logbook 它相关的功能了，我们再取消注释，然后再记得没有变更，我们看它又可以，它又回来了，这个 dependency 呢？dependency 去对应这个依赖项，这儿它是一一对应的。

说话人 1 00:06:56
我们再看一下这个 plugins 对应的是这个插件。因为我们这个 spring 它的初始化已经帮我们自动的引用了一个 plugin， 这个 plugin 是在这个位置， spring boot 这个就是这个插件是对应的。然后为什么这个插件不止一个呢？因为前面这些插件是 Maven， 它自动帮我们导进来的插件，这个插件是我们这只有一个 plugin， 它对应的是这个 spring boot 的一个 plugins， 我们可以看到这个 or spring framework。

说话人 1 00:07:32
和这个是对应的，说明这个 Plug in 对应的是这个插件，这个插件提供一些功能，我们就是可以直接用，但是我们这个功能我们还不怎么用得到，我们现在点击一下这个启动，这个是测试类，我们点击下这个启动类，点击运行，点击运行以后它会帮我们把 Java 源代码进行一个编译。

说话人 1 00:08:09
这个 XY 的文件夹，这多出来就是它帮我们编译出来的字节码，我们都知道我们写的是点 Java， 然后。它编译出来以后就成了点 class， 我们可以看一下， OK， 我们先不用这么多依赖，这，我们先都把它注释掉，只保留 Web 这个 longbook， 留着 test 也可以留着加载 Maven 变更。现在我们只保留了3个依赖。

说话人 1 00:08:53
我们点击一下运行，哦，点错了，应该点这个是，这个才是启动类，我们会看到已经运行起来了。这个就是服务已经在启动了，已经启动好了，我们。可以看到这个 target 文件夹下面是有一些已经编译好的字节码，这个就是点 class 文件 Maven， 它是把所有的这些 src 下面的我们写好的资源都是编译成字节码以后放到 target 文件夹下面。而 Maven 这个 clean 就是我们很常用的，它就是可以把这个 target 文件夹删掉，我们可以试一下点击下 clean， 然后它有了，然后我们再运行。运行完以后，它又会帮我们重新编译这个项目，编译成一些 class 文件出来，然后又放到这个 target 文件夹下面，然后我们再 clean， 然后这个 target 文件夹又可以被删掉了。

说话人 1 00:10:23
其实这个 classes 这个文件夹下面不只是 Java 编译好的字节码，像这个 resources 文件夹下的东西，它也是放到了 classes 这个文件夹下面，不知道你们有没有听说过，就是在你们在引用文件的时候就有一个 classpath， 那个 classpath 对应的其实就是这个 classes 文件夹的这个位置。像这个 application 的 property， 它就是在这个 classes 文件夹下面，所以可以直接用 classpath 下边的这个 application 的 properties 引用到这个文件，我们再看一下这个配置类配置文件，它默认提供的是点 properties 配置文件，但是这种格式其实不太好用，我们可以给它改成。

说话人 1 00:11:15
YML 类型的文件，我们把它删掉，其实 properties 配置文件和 Yml 配置文件其实都可以用来配置，但是 Yml 类型的配置文件它是更加的方便一些，我们新建一个文件 application 点 Yml 新建项目以后肯定是要先写些东西的。我们上来就先把它的端口指定了 Server， 然后点 port， 它默认是8080，你可以改成自己喜欢的，例如我这写8080，还有一个是 spring application， 还有它的 name 诶，写错了，这个我们是 message broker， 所以我就写的 message broker。现在我们来重新启动一下，发现这个 Tomcat initialized with port 8080，这就是我们写的端口，但是我们不写8080，我们就改成9090，我们再看一下它启动起来是在哪个端口上启动的。

说话人 1 00:12:57
现在我们看到他们开了，是在9090端口上启动的，那我们改成0呢？试一下0其实虽然是0，但它其实不是真的在0上面启动，它可以选一个没有占用的一个随机的端口。我们还是改成8080吧，如果你电脑上8080端口被占用的话，那你就不要写8080了，你可以改成其他的，只要你电脑上这个端口没有被占用就可以了。

说话人 1 00:13:46
现在我们这个项目就初始化完成了，我们接下来就是讲一些我们一会要用的一些工具，像 Lombok， 然后像 Jackson， 它可以帮你把一个类序列化成 JSON 文件。接下来我来讲一下非常好用的工具，叫 lombok， 我们会看到我们之前也把它引进来了，这是它的依赖， Lombok， 依赖它现在在 POM 的 XML 里面是有的。就是这个东西为了验证它多好用，我们先创建一个 main 方法，再来创建一个用于测试的 person 类。一个 person 应该是有名字。还有年龄，现在我们看创建一下，发现他没有这个全参构造方法，只有无参构造方法，我们只能回去再点击一下生成构造函数，把这俩勾上，现在他就可以了，不报错了。但是你要什么都不写的话，他就没有无参构造方法了，你还得回去生成一下无参构造方法。

说话人 1 00:15:28
现在它也不报错了，我们来打印一下，看一下它打印出来的效果是什么样，就发现它是一个类名，然后艾特，然后后边是一串字符，这显然跟我们的填进去的这个小名和18丸就不是一个东西，说明我们还没有重写 tostring 方法，我们再来重写一下 tostring 方法。生成，然后 tostring， 我们再来运行，发现它这回就正常了，这个是它的类名，这里边 name 等于小明， age 等于18，就是我们刚才填进去的属性，我们来看一看。两个完全一样的人，是否能不能正常判断相等？

说话人 1 00:16:45
你会发现它竟然不相等，明明名字和年龄都一样，它们竟然不相等，是因为我们还没有重写 eqals 方法。我们看他们的 hashcode 是不是相等，他们的 hashcode 竟然也不相等，我们来重写下 equals 和 hashcode， 再来看一下。

说话人 1 00:17:43
对。回 equals， 返回 true hashCode 也相等了，我们想要改它一些东西，我们看有没有 getter 和 setter？set 不没有 set， get 也没有 get， 我们想要生 getter 和 setter 的话，还得回到这个类里面去给它生成 getter 以及 setter。

说话人 1 00:18:17
现在我们就有 getter 和 setter 了，我们现在再试一下有没有 get。有，有没有 set？也有。为了让一个类能够正常使用，我们做了这么多工作，只有两个属性，他就后边这么多，包括构造器 tostring、 equals、 hashcode， 然后 get 和 set 画了这么多代码，其实我们这些都不用，我们只需要一个注解就够了，我们加一个 attribute 注解，我们看一下现在它是不是可以正常的用 get 和 set。有 get 啊。

说话人 1 00:19:00
也有 set， 我们来试一下 get， 然后试一下 set， 看一下它们是不是相等的。先看它打印出来效果，我们没有写 toString 方法，它也可以这样打印，我们看它是不是相等的，但是它也确实是相等的，看看它们的 hashcode 是不是一样。

说话人 1 00:20:14
它是 code， 也一样，是不是没有问题？我们看它有没有全参的构造方法发现它。没有全参构造方法，它上面写因为0个实参，那实际为两个，所以我们在里边再加一个叫 allargsconstructor， 现在它就全参构造方法了，但这又出问题，他说因为两个实参，那实际为0个，因为你标注完这个 allargsconstructor 以后，他这个无参构造器又没了，我们再来一个 noargsconstructor。

说话人 1 00:21:10
好，现在这么一搞，他的无参构造器也有，然后全参构造器也有，所以你会经常看到这三个注解，它就是可以帮你简化无参构造器、全参构造器以及 getter， setter、 tostring、 equals， hashcode 等各种。每个类一定要有的方法的这些编写，你就只用管这些属性就可以了。这两天 Kate set 也不用操心，它已经帮你生成好了，我们现在来看一下如何把它换成转换成 json 的。

说话人 1 00:21:44
我们用的 JSON 就要用 objectmapper， 我们先创一个 objectmapper， 然后里边有一个方法叫做 writevalueAsString。我们把 person 传进去，但发现它是有一个未处理的异常，就是一个 JSON processing exception， 它是一个检查型异常，我们要么添加到符号签名，要么对它进行捕获。但还有一种方法就是最简洁的方法，你可以直接在这更多里面选择。在 main 方法上添加注解 sneakythrows 可以把这个检查型异常包装成非检查型异常，这样就不会报错了。

说话人 1 00:22:42
我们看一下他能不能把字符串，能不能把这个对象变成 json， 发现他确实可以变成 json， 这就是 JSON 格式的。

说话人 1 00:23:01
那我们现在来研究一下他能不能把这个 JSON 还原成他的这个对象呢。

说话人 1 00:23:12
我们仍然用的是 objectmapper， 这次用的叫做 writevalue。readvalue 说错了， readvalue 只有两个参数，第一个参数写你这个字符串，第二个参数写你要转化成谁，那成哪个类？他这个 class， 我们这个 JSON 字符串在这，然后 class 是要转成 person， 所以这写 person 点 class， 我们现在看一下，看能不能转化成 person。

说话人 1 00:23:57
运行发现它成功的把这个 JSON 字符串转化成了这个 person 对象， long book longpo 还忘讲了一点，还有一个叫做。SLF4J的一个注解，把它标注在类上，你可以直接用 lo，然后 info warn， 然后 error debug 这些可以，还有 trace， 你可以直接打印日志，假如我们不加你这个 lo，你直接报错的，你就没法用 log info 打印任何日志，我们把这个注解加上，我们再用 lo info 试一下。

说话人 1 00:24:59
可以发现他把时间还有这个级别，当前这个类以及内容都打出来了。如果我们不用这个 loginfo 的话，他就只会打这个内容，而不会打前面这些信息。这个 log 它也是有不同级别的，看下不同的级别。war 还有 error。

说话人 1 00:25:48
看看他们的。级别也是不同的，像这个是 info， 然后 warn error 其实还有更多的级别，像 debug， trace 什么的我就不演示了，现在我来演示一下如何用占位符来打印日志。假如说你日志有很多个参数，就用这个来占位，然后你在后边的参数列表里边再写出这些参数是什么，然后第一个参数就是。123，然后第二个参数是456，我们看一下什么效果可以发现它把这个123放到这个占位符里面，然后把456放到了这个占位符里面，所以你这个一个字符串后边接几个这个参数，它就可以自动帮你填充，这样的话就相比你这样直接用加号来拼接的话，就会看上去更加舒服一些。

说话人 1 00:27:04
这节课我们来讲 spring 的 IOC， 我们先在这个。messagebroker 这个包下新建一个软件包叫做 entity。我们创建实体类，先从 school 开始就有一个学校有它的名字，当然我们还是肯定是要加上 data 注解，这样我们的 getter， setter 都不用我们自己写了。一个学校应该是有。有学院。college 应该还有宿舍 dormitory。

说话人 1 00:28:04
我们现在接下来创建学院和宿舍，有学院，学院里面应该是有他的名字。还有学院里边应该是有学生。

说话人 1 00:28:34
把宿舍建好， dormitory 宿舍也是要有名字，宿舍里面也有学生，还是要加上 data 注解。对，这个也要建成 data 注解，现在创建一下，学生也有他的名字。以及 data 注解，现在我们四个实例创建好了，我们理一下它的关系。这是学校，学校里边是有学院还有宿舍，然后学院里面是有学生，然后宿舍里面也有学生，然后这是学生。我们创建一个 main 方法，我们来看一下，假如不用 spring 的 IOC， 那是怎么样管理它们间的依赖关系。

说话人 1 00:29:49
首先我们需要创建一个学生 student， 导一下这个类，设置它的名字，接下来创建学院。设置学院的名字，然后把学生设置到学院里面。set student， 现在创建宿舍，设置宿舍的名字，然后把学生录入到宿舍里面。之后创建学校，这是学校的名字，然后设置学校里的学院，以及学校里的。学校里的宿舍换几个？行，这个是学生创建，这个是宿舍创建，这个学院创建，这个宿舍创建，这是学校创建，这样给分开学生、学院、宿舍，还有学校。最后经过这么多代码以后，我们现在创建好了，现在看一下，打印一下它们之间的关系。

说话人 1 00:32:20
这是学校里面有学院，然后里边有宿，学校里边有宿舍，然后学院里边是有学生，宿舍里边也有学生，然后这两个学生其实是一个人。可以发现我们是 new 了这么多次，一次、两次、三次、四次，然后还调用了很多的 set 方法，包括这个 set name， 然后 set 它们之间的依赖关系都是我们主动的 new 出来，然后去 set 的，这样的话就比较麻烦，而且它们的依赖也比较紧，现在我们来看一下 spring 是怎么管理它们之间的依赖的。首先我们需要把它注入到容器里面， spring 有个 IOC 容器，我们需要用 component 注解，我现在先用。加一个 component 的注解，把它交给容器里面，点错了，应该是点这点击运行。

说话人 1 00:33:44
现在我们来看一下里边有没有这个 bean， 有，在这里这个界面显示的是 IOC 容器里面所有的 bean， 这个 school 已经注入进去了，我们再来注入一些其他的来看一下。比如 student， 现在这里面是没有 student 的，加上 component 注解，然后重启一下，现在这里边这个 school 只有 school， 没有 student， 看一下有没有 student。

说话人 1 00:34:57
有 student 了，然后这是 school， 然后这是 student， 说明这个 school 和 student 都已经被 spring 注入到这个 IOC 容器里面去了。我们也把其他的加上 component 注解，让这些类都被 spring 托管。

说话人 1 00:35:22
至于它们之间的依赖关系，我们可以用 value 注解来设置。依赖关系用 autowired， 然后他们的属性用 value 注解，像这个 new 我们就都不用 new 了。new 不用 new student set name， 我们来到 student 上，用 value 注解，设置一个 name， 然后 college name 和他们之间的关系，我们来到 college 上面。

说话人 1 00:36:16
它们的关系， autoWire 现在我们来看 dormitory set name 和 set student， 我们也是用 value 和 autoWire name 上面标注 value。

说话人 1 00:36:45
这个上面标注 autoWire 以及最后的 school 的 setname 和 school 的 set， 它们的属性来到 school， 下面标注 value， 以及这个上面标注 autoWire， 这个 main 方法我们可以删掉它就没有用了，因为我们现在已经把 bin 都装交给了 spring 来帮我们管理。我们现在重新启动一下，看一下这里面有没有我们这些 bin 刚才注入进去的，点几下这个图标，让它刷。

说话人 1 00:38:00
新一下还没有，有了。这个 college 就是我们注入进去的学院，这个黄色背景都是 spring 他们自己的类。然后这个黑色背景就是我们自己注入的类，这个 college 看一下 college， 然后 dormitory 看一下 dormitory。看一下 school， 然后看一下 student， 说明它已经都被 spring 管理了。我们来到启动类上，在这个方法后面用点 run， 它会帮我们返回一个 applicationcontext configurableapplicationcontext， 这个就是我们的 IOC 容器了，我们可以直接从这里边。拿出我们要的 bin u context， 点 get bin， 我们拿 school， 拿 school 里边传入进去是 class 对象参数写 school， 点 class 在这是个断点调试，看一下这个 school。里边到底是什么东西？

说话人 1 00:39:56
这 school 里边 name 已经有了。school name 是我们用 value 注入的属性。看一下 college 也有了，然后 dormitory 也有了。看一下，点开看一下 college， 它的 name 也有，然后 dormitory 它的 name 也有，看一下 student。展开 student， 它是也有 name， 注意，需要注意的是这个 student 后边 AT 7143，然后这是 AT 7143，说明它们是同一个实例，它们是注入的是同一个 student。因为 spring 它创建 bean， 它默认是单例的，它创建完 student 以后，它这个 college 引用了这个 student 和 dormitory 引用 student， 它们是同一个，现在我们看一下我们写的这几个类，我们有用 new 吗？没有用 new， 一个 new 都没有写。然后我们也没有用 set 注入值，就是我们把之前的用手动 new 出来，然后用 set 注入，我们都给它省略了，我们把它全都交给 spring 容器管理，我们只要需要在这个类上加一个 component 注解，然后你要的那些属性可以用 value 注入，也可以用 autowired value 就是注入那些比较基本的值，然后 autowired 是注入引用的你自己的定义的那些类，像 college dormitory 就 autowired 注入，现在就是用 spring 这个 IOC， 可以帮我们简化它们之间的依赖关系。

说话人 1 00:41:28
虽然我说是用 component 注入，但是你用 component service， 然后这些都可以看一下一共有哪些 component 是可以的， service 也是可以的， Controller 也是可以的， service 也可以，然后 repository 可以。

说话人 1 00:41:58
这四个注解，它们都可以完成一样的功能，就是标注在你要的类上，然后 spring 容器就会把它。实例化出来，然后放到 IOC 容器里面，这些关依赖关系都由 spring 来管理了。这四个虽然表面看着不同，但是这些不同也只单纯的只是让人看着方便，是标注这是哪一类的，功能是什么样的。但是你把 component 换成 service 都是完全不影响的，因为它只是起一个让人看得清楚而已，但它功能是一模一样的。可以试一下，你可以把这个 component 全换成 service 这四个注解，它们只是单纯的名字看上去不一样而已，但实际上它们功能都是一模一样的。看一下 school， 然后 college dormitory 其实完全一样，跟之前有这些注解以后，它都是 spring 管理了这些 bean。

说话人 1 00:43:15
我们现在这个 value 是直接写死的，其实我们还可以从配置文件里面读取配置文件就是我们这个 application 的 yaml， 我以这个 school name 为例，我这里边创建一个叫做 school name， 它会标黄，不过没有关系，你把这个 value 里边不直接写，直接 dollar 符号，诶？dollar。

说话人 1 00:43:57
括号，然后一个大括号里边就是写这个配文件的值，中间这个多级用点分开，像这个就是 school 点 name， 我们再重新运行一下，看一下 school 这个名字，从配置文件中读取的名字，这就是我们这个的名字，现在没有标黄的警告了。因为我们这个 value 里边 dollar， 然后大括号里边就写的是配置文件中的一条路径，它会按照这个路径去配置文件里面读取，这样的话我们也可以把其他的几个都改成从配置文件读取。配置文件就是我们这个 application 点 YML。rvalue 就是注入里面的属性值的，还有一个地方就是有一个 scope 注解，这个不是很常用，因为默认的它单例就已经符合我们的要求了，它默认的 scope 是 Singleton， 就是单例的，不管你从这个容器里边 get 多少次。它返回的 bin 都是同一个 bin， 我们来做个实验，你从这里边拿一次，再拿一次，在不加这个 scope 注解的时候，它默认就是 Singleton， 现在我们是从里边拿了两次 get bin， 看一下SCHOOL1，这个是5948，然后 SCHOOL 25948。它们两个是完全一样的，说明它们只说明这个 school 只被实例化了一次，然后两次 getbin 都是取出来是这一个 bin， 我们现在把 school 改成 Singleton， 其实还是没有变化，因为它默认就是 Singleton。

说话人 1 00:46:40
看一下这个 SCHOOL 1594一 SCHOOL 2594一它 getbin get 了两次，结果返回是同一个对象。我们现在来把这个 scope 的这个改成 prototype 试一下，我们再来试一下他们返回的两个到底还是不是同一个。

说话人 1 00:47:15
看一下 school 一是5941，然后 school 2是5942，说明他们返回的并不是同一个。我们用这个注解里边写一个 prototype。它会导致每一次 get bin 返回的是一个新的。它们两个不再是一个病了，我们先把之前创建的这些先恢复一下，恢复到原来的样子。像 college 也改成还是改成 component？

说话人 1 00:47:54
像这个 scope 我们就给它删了，我们默认用单例的就可以，这个也恢复成。写好的一个值，我们刚才都引用这个 component 注解，让它把这个 bin 交给布容器托管。如果我们把这个注解删了会怎么样？我们可以试一下先给它注释掉，然后我们启动可以看到它这个 require a bean of type， 说明这个 student 这个类找不到，就 IOC 容器里边找不到这个类，因为我们把这个 component 注解删了以后，这个。student 这个类就不会被 spring 托管，然后其他的在装配的时候也就找不到了。可以看一下 college 这个已经标红了，说明上面已经标了，无法自动装配，找不到 student 类型的 bean 这个 ID， 它是也有这个报错报警，我们再把这个 student 这个 component 给恢复。

说话人 1 00:49:14
现在它就可以装配了，点一下这个按钮可以导航到自动装配的依赖项，可以看到这是导航到了 student 上，那如果我们不用 component， 又让它注容器，有，还有其他办法吗？也是可以，我们可以用配置类，然后用 bean 注解，把它注入在这里创建一个配置类，这个类名就可以随便起，这无所谓。

说话人 1 00:49:48
配置类上面是要加 configuration 注解的，表明它是一个配置类，然后我们用 bind 注解返回类型，就是写我们想创建交给布容器的这个类型，这个 bind 是什么？然后名字就写 student， return 一个 new student。我们现在看一下这个 student， 它是没有用 component 注解的，现在还可不可以发现它是可以的，因为我们这个 student 类型的 bin 已经被注入到了 IOC 容器里面。导航一下看看它从哪注入的？它是从配置类里面注入的，这个配置类里面的方法用 bin 修饰以后。

说话人 1 00:51:00
这个返回的类型就是我们注入 IOC 容器的这个 bin 的类型，这个方法的名字就是这个 bin 的名字，我们点一下这个 activator， 可以用这个显示库 bin 把它关掉，它就不会显示这么多。我们不是我们自己创建的 bin 了，这有一个显示库 bin， 把它一关就只显示我们这几个自己的 bin， 看一下 scope。

说话人 1 00:51:34
部，它是依赖了 dormitory 和 college， 然后我们看一下配置类，这个 student 装配到了 dormitory 和 college 里面，然后这个 student 是由这个 myconfigconfiguration 这个配置类创建出来的，我们用 atbin 这个注解就可以创建一个 bin， 就可以看导航到自动装配候选项，可以看到这两个类装配了这个 student 类型的 bin。除了 configuration 里边用 bind 注解把一个类注入到我们的 IOC 容器里面，还有一个方法利用 import 注解，然后加到配置类上面，然后用 import 后边写的是 class， 我们要注入的是 student 类，然后我们就用 import student 点 class， 我们先试一下，我们既不用配置类，也不用 component 的注解，也不用 import， 看一下。

说话人 1 00:52:45
我们先不加 import 注解是什么效果？发现它报错了，说一个 student 它找不到，因为我 college 需要一个 student， 但 student 它在这个容器里面没有，所以它就报错。我们用 import 注解，把它导入一下，看一下效果，现在它又正常启动起来了，说明容器里面是有 student 这个 bean 的，我们看 actuator， 看一下我们这里面有哪些 bean。通过 myconfiguration 这个配置类里面看一下这个 school， 然后 college student 它是有的， student 它是在这个容器里面的，而我们这个 student 它没有用 com， 不用 component 注解，那不是 student， 它没有用 component 注解，但是它也可以在容器里面，就是通过我们这个。

说话人 1 00:53:59
import 注解导入进来的，所以创建一个 Bean， 你可以在你这个 Bean 这个 pass 上面加一个 component， 也可以用配置类，然后 configuration， 然后里面用 atBean 注解这个方法返回的一个 Bean 也会被装配到这个，注入到这个容器里面，或者是你用这个 atimport 里边写一个你要那个类的 class， 也可以把这个类注入到容器里面，这几种方法都可以完成一个 Bean 的注入。这节课来讲一下，有很多类都符合装配要求的时候怎么办？我们先把上节课创建这些类都删干净，现在只留一个启动类，我们在这儿创建一个类叫做 calculator， 然后创建一个软件包，里边有个接口。

说话人 1 00:55:14
然后这里边接口里边有个方法叫做。calculate。

说话人 1 00:55:31
传进去两个 double a， 然后 double b， 再来一个软件包里边 IMPL， 就是。记它的实现类，假如说有个 add 类，就实现了这个接口，实现这个方法给它加一个SLF4J的注解，这样他让他打日志，这个类的实现的方式就是把它们加起来，然后打印出来。

说话人 1 00:56:29
这里面可以装配一个这种接口类型的，然后这里边有一个方法，它可以调用这个接口类型里面的计算。

说话人 1 00:57:14
我们想装配，所以需要 autowired， 以及这个类也需要注入 IOC 容器里面，所以把它加上 service 注解，加 component 注解也可以，因为这几个注解它们作用都是一样的，这个也需要加上把这个 calculator 这个类也让 spring 托管，现在可以看到这有一个可以导航到自动装配它导航到了这个 add， 因为这 add 实现了这个 operation 这个接口。然后这个 calculator 正好需要一个实现 operator 接 operation 接口这个类，它这个 auto wire 是可以作用在接口上的，它会找一个实现这个接口的类装配进来，直接后边用点 wire， 然后获取这个 IOC 容器，用 getbin 方法我来获取一下这个 calculator 这个类型的 bin。点 wait 调用一下这上面的方法。

说话人 1 00:58:52
运行一下可以看出它执行了这个 add 的操作，它这个实现类，它装配进去是 add 这个类，因为这个日志上也显示出来了是 service， 然后 imp l 底下的 add 这个类，你假如说我们再来一个实现类，先把它停掉。

说话人 1 00:59:43
这个类它也实现了这个 operation 的接口，实现它一下这个方法，不过这回我们就不是加了，这回来给它让它减，必须要在这个类上加上SLF4J注解，这样才有日志的功能。然后这个也用 service 注解，把它注入到这个容器里面。我们现在来运行一下，我们看一下它报错，它报的是什么呢？fail field operation， 然后是。require a single bin， 然后 but two was found。他找到了两个，一个是 add， 然后一个是 substract， 说明有两个类都符合要求，所以他不知道该注入哪个了。

说话人 1 01:00:54
看一下他这个报错，无法自动装配存在多个 operation 类型的 bin， 那怎么来指定呢？我们可以用这个方法，假如说我就要 add， 可以在它这个类上用 primary 注解标注一下。primary 就是主要的意思就是存在多个时间类的话，会优先选 primary 注解的。现在我们来看一下，现在它已经成功运行了，它调用的是这个 add， 假如说我不想要 add， 那我们把它换到 subtract 上面，再来看一下它这回调用的是什么。

说话人 1 01:01:56
他这回调用的是 substring 这个方法，因为我这个上面标了 primary， 所以他会优先选择这个类。

说话人 1 01:02:09
我们看一下他注入的是 substring 这个方法，那还有一种方法就是直接用 qualifier 注解。我们现在两个都不加 primary 注解，现在它又开始这里开始报错，说无法自动装配存在多个 operation 类型的 b， 我们用 qualify 注解给它指定一下，我们就要 add， 现在它也不报错了，我们来看一下它会导到哪？导到 add 上面我们来重新运行一下，因为它确实是 add， 我们来看一下它装配的是什么。

说话人 1 01:03:16
装配的是 ARD， 没问题。那我们把这个 qualifier 然后换个名字再来运行一下，现在它适用了 substract 这个类，来看一下它是不是。确实是装配到了这个类上面，和这个 qualifier 它的里边的传的类名是一样的。那这个类名是什么？怎么定的呢？其实类名就是它这个类的首字母，把它变成小写就是了。这 ARD 把这个第一个 a 变成小写，那它这个。name 就是小写的 add， 然后这个 subtract 它这个名字把第一个 s 变成小写，那它这个名字就是 subtract， 这就是它的名字，你也可以指定，如果你不喜欢这种默认的名字的话，你可以指定你要自己的你喜欢的名字，像这个 service 后面可以给上 name， 不用跟 name 直接写就行，直接写。因为你可以写一个自己的喜欢的名字就行，就用 my name 来举例，这样他的名字就改成了 my name。那我们现在再用 substrait 装配一下，看可不可以。

说话人 1 01:05:21
现在发现它装配不上了，看一下报错原因，因为我要的是 substract 名字的 bean， 但是你给的名字你写的是 my name， 所以它就不叫 substract 了，你想要装配它，你也得改成 my name， 在这个 qualifier 里边改成 my name。

说话人 1 01:05:54
现在它又可以出现导航到自动装配的选项了，我们看下导航到哪？导航到了这个 subtract 上面，因为我给它起了名字，就叫 my name， 我们再来运行一下，现在它就成功地运行了，也没有报错。如果你想添加新的方法的话也很简单，就在这里边新建一个类，然后例如乘法吧。开始喽。猫。

说话人 1 01:06:41
然后这个类也是要实现这个 operation 接口，实现它的方法，为了打日志，我们需要建SLF4J一个注解，我想要指定要乘法。那我们就把它改成 Multi。multi。懂了，这个还需要先把它注入到这个容器里面，用 service 注解或者 component 注解，然后或者是 repository 注解、 Controller 注解也可以，因为它们都有一个意思。

说话人 1 01:07:40
我们再来回到 calculator， 现在看一下它导航的选项，它会导航到这里，因为它一会要装备到这。我们重新运行一下，看一下它装配的是什么？它装配的确实是我们要的这个东西，当然你也可以不用 qualifier。你用 primary 也是可以的，现在再试一下，它现在也是成功的装配了我们需要的 bin。

说话人 1 01:08:38
接下来我们来讲如何编写 Controller 之前，我们先看一下这个 HTTP 请求的格式怎么样的，这是请求报文，然后这是响应报文。我们就先重点关注请求报文，它有三部分组成，一个是请求行，一个是请求头，一个是请请求体。请求行里面有方法 URL 和版本，其中这个 URL 里边是可以后边带信息的，你可以用问号，然后 a 等于 b， 然后用 and 符号，然后 c 等于 d， 这样可以把你的需要的信息写到 URL 里面，一会我们可以用 request param 数也把它提取出来，然后这部分叫做请求头，你可以用首部字段，然后冒号后边空个格。然后后边有个字段值这个，这部分请求头里就是 header 部分，一会可以用 request header 把它提取出来，用 request header 注解。然后这一部分是请求体，我们用请求体一般都是用 JSON 格式的，对于 JSON 格式的请求体，我们可以用 request body 注解，把它提取出来。

说话人 1 01:09:52
在 API Fox 中， query 参数就指的是 URL 中的参数， header 参数就是指的这一部分。请求头用的参数，然后 body 参数就指的这一部分请求体中的参数，我们可以看一下 API Fox 和它们的对应关系。API Fox 这个新建一个，这是 API Fox， 我们新建一个快捷请求这个 param， 看一下 param 对应的就是 URL 中的，然后 body 对应的就是这部分数据中的。然后这个 header 对应的就是这个请求头中的它们是对应的 param， 然后 URL， 然后 body， 然后这个请求体 header 请求头，它们这三个是在这个请求报文的不同的地方，所以我们不应该把它们搞混，我们在这里边新建一个软件包叫做 Controller， 然后新建一个类就叫 my Controller。

说话人 1 01:11:14
Controller， 我们在前后端分离的项目中，我们是用 rest Controller 注解，这个注解是普通的 Controller 注解，然后和 response body 注解的结合，这个注解就包括了 Controller 注解，还额外添加了 response body 注解。

说话人 1 01:11:43
来建一个 hello world， 我们先用 get mapping， 然后这里边返回一个字符串，现在一个 Controller 就写好了。这个 get mapping 代表的是我们，它接收的是 get 请求，然后边什么都不跟我们看一下这是怎么访问的？

说话人 1 01:12:33
默认它是在8080端口，我们可以看一下它是在8080端口，我们用 local host 8080看一下 hello world， 这是我们刚才写入的内容，我们也可以改成其他的，我们再重新启动一下。

说话人 1 01:13:16
那已经启动好了，我们再来刷新一下，可以发现它的内容也会跟着变，我们。我们看一下它这个端点，还没有端点，那我们来给它加一个默认情况下，这个不加，就是在项目的根目录下不加，就是和 get mapping 和这个后边加一个斜线是等价的，我们也可以在后边写一下你要的路径，例如 user 那 API， 例如 API。然后V1漏重新启动一下项目，这个时候我们这样访问就访问不了了，我们需要在后边加上 api， V1， hello。这样就可以访问到了。

说话人 1 01:14:43
当然浏览器它有个局限，就是你这么搞的话，它只能发送 get 请求，我们想用 get， 不止，我们肯定不止用 get 请求，我们还需要用 post 请求，包括 put 和 delete 请求，这样的话浏览器它就搞不定了。我们这时候可以用到 API Fox 来模拟我们的请求，这里写127.0.0.1 local host， 当然也可以，因为 local host 它就是指127.0.0.1，我们点击发送。

说话人 1 01:15:22
要加这个8080端口，可以看到他返回了，我们在这设置的字符串，我们看一下他有哪些请求方法， get post、 put、 delete 这么多，当然这有相应对应的 Mapping， 我们把 get 改成 post Mapping， 试一下重启项目。

说话人 1 01:15:58
现在你再发送 get 的话。它就已经看一下它这个，哎，连不上，连上了会发现这个 method not allowed， 说明它不支持 get 方法，我们这时候需要把 get 改成 post 发送，发现可以正常返回，我们再把它改成。put 方法试一下，当然这个也要改成 put， 发现 put 方法是也可以正常返回的，我们再来改成 DELETE 方法试一下。这个也可以改成其他的，回到 API Fox， 我们这回用 DELETE 方法发一下。本地的方法它也可以匹配到。我们在写方法的时候，如果每一个方法都是会在这个方法前面这个 Mapping， 后边跟 API V1就太麻烦了，就有很多重复的地方，我们可以把这个 API V1提到这个类上面，我来演示一下类，下面有一个叫做 request mapping 注解。

说话人 1 01:18:29
它这个方法其实就相当于把这个 get mommy 里边这字符串去和这个类上面的 request mommy 进行拼接，我来给它改一下，然后我们一会看一下效果，重启一下项目。

说话人 1 01:19:19
我们发送发现是可以，我们发送的是 API V1 hello， 但是我们这个 gate Mapping 后边只跟了这个 hello， 它就可以匹配到，说明它是。把这个 Git Mapping 上这个 hello 去拼到了这里，它是这么匹配到了APIV1hello，我们可以把这个类里面的这些请求的前缀提到类上面，用 request mapping 注解里边写上我们这个通用的前缀，这样可以在每方法里面就不用写这个通用的前缀了，我们试下其他方法， post 也没问题， put 也没问题。现在我们就先只保留一个，现在我们来讲一下这个方法里面可以跟着的一些东西哦。

说话人 1 01:20:16
对，还有我们这个在接口测试工具的时候，如果每次都写这个 API V1就很麻烦，我们也可以把这个接口测试工具里边这个前缀提到环境里面，这有一个请选择环境，我们点管理环境，可以把这个粘到这里来。这个要写8080端口，这个端口你们是在什么端口上跑起来，你们就写什么端口，点击保存，我们设置了测试环境，这个前置 URL 是127.0.0.18080里边有个 API V1，我们把这个环境选到测试环境，我们这里只写 hello， 看一下效果，发现是可以正常请求的。既然 get 也可以，既然 post。也可以，相当于我们这个 URL 写这么短，也非常请求，它的原理就是它把这个前缀都写到这个环境里面，所以像这么长一串的前缀，你就不用在这个里边写了，就非常的省事。

说话人 1 01:21:38
我们继续来讲 Controller， 这个是我们刚才建立的 Controller， 我们先讲如何提取 header 参数，我们来看一下 API Fox 这里边有个 header， 这里边就是 header 参数，我们可以用 request header 这个注解。来提取 header 参数，我们来运行一下这个方法，这个字段名叫啥？然后我们这个 header 的 key 就设置成啥，来试试一下这个 header 里边写假如说12345678，然后这个还得先输一下，这个是 post。然后这个是 hello， 我们在这个环境里边已经配好了它这个前缀，它会把这个 hello 拼接到这个前面这个 API V1，后面我们看一下它提取到了是12345678，用这个 request header 注解。

说话人 1 01:23:02
我们来看一下，如果不加这个会怎么样？不带这个 header 发送，发现它有一个 bad request， 就是出现了错误，因为这个 request header 它是默认是必填的，如果我们想让它不填也行，我们可以这样一个 require 等于 false， 我们再试一下。

说话人 1 01:23:45
发送这回就可以了，我们没有填，它也可以正常请求成功，这个时候它就是绑定为了 null， 那假如说我们不想让它是 null， 我们想给它个默认值怎么办？我们来看一下这个，我们不仅可以指定 require 等于 false， 还可以给它加上一个 default value， 我们来看一看它这个默认值能不能正常使用，我们重启。

说话人 1 01:24:26
我们这个是没有带这个参数的，也是请求成功，看一下它这个 authorization 是默认的 authorization， 因为我们是指定了 default value， 我们讲完了如何提取 header 参数，就是用这个 request header 注解，我们接下来讲如何提取 query 参数，我们提取 query 参数是用的这个 request param 主如何提取 query 参数？用的是。

说话人 1 01:24:58
requestparam 这个注解，我们重启一下，我们这是因为这个是 name， 所以我们要提取的 parame 参数，那个 query 参数的 key 就是 name， 这个 header 我们把它给删掉，我们现在去测试 param， params 这个 query 参数它是写在 URL 里的，我现在可以写一个，然后你们看一下效果。用问号，这个 URL 后边有个问号，然后 key 等于 value， 我们发送一下可以看到请求成功，我们看一下控制台的打印，可以看到他的 name 是小明，就是我们刚才写进去的值。如果我们不写，我们把它不启用，它就不会带我们发送一下，发现有一个400的 bad request。因为它是报错了 missing serverlet request parameter exception， 因为这个 request param 它默认是必填的，如果我们想不填怎么办？把它注释掉，看一下，这有一个 require 等于 false， 我们写一下 require 等于 false， 再看一下效果发送，这回是请求成功了，我们没有写这个 name， 它也可以请求成功，在这个时候它的 name 就是 null， 如果我们不写的话，这个 name 就会绑定为 null。

说话人 1 01:26:51
如果我们想给它设个默认值，我们可以用这个 default value 给它设置一个默认值，这样我们不写的话，它这个 name 就会绑定成默认值，我们重新启动一下。

说话人 1 01:27:21
再发送，这回是请求成功，我们看一下控制台打印什么 name 是默认的 name， 这正好是我们写的这个默认的 default value， 那这个 requestparam 可以接收我们自定义的类呢类吗？其实不可以的，我们试一下就知道了。这个 userparam 是我自己写的一个 param， 我们看一下项目结构，这个接收的从前端往后端传的话，我们都会放在这个 param 文件夹里面。这个就是我写了一个接受的一个类，用 data 注解，让它帮我们生成了 get 和 set， 这样我们就不用自己写 get 和 set 了，它这里边有两个字段，一个是 name， 一个是 age， 我们在里边添加这两个 query 参数，我们看一下它这个 query， 这个 URL 是怎么写的？这个问号，然后后边是 name 等于小明，然后 and 符号，然后 age 等于18。

说话人 1 01:28:31
如果你还有第三个参数的话，也可以继续在后边加，然后 and 什么等于什么，它就这样一个问号分隔前面和后面，然后后面两个不同的参数之间是用这个 and 符号来分隔的，我们来发送一下，看可不可以接收，哎，可以吗？

说话人 1 01:29:02
竟然可以，我没有重启，我重启一下，我们现在来发送一下，发现它是有个400的 bad request 的报错。这说明我们用自定义类的话，我们是无法用 requestparams 来提取我们在这个 URL 中的那些 query 参数的。那我们怎么才能提取？其实也有办法，我们这样，我们不加这个 query 参数，不加这个 requestparams 注解就可以了。

说话人 1 01:29:54
我们现在重启一下项目，我们再发送一下。这个时候请求成功，有 userparam name 有，然后 age 有，如果我们其中有个为 null， 可以吗？我们试一下，我们就不填 name， 发送发现是没问题的，那就 name 绑定为 null， 然后 age 是18，那么不填 age 可以吗？也是可以的，那就 name 有，然后 age 是 null。那两个都不填可以吗？发现也可以，那就两个都是 NULL， 那怎么给它设置默认值呢？其实也有办法，你先给它设置默认值，你就这么在这写一个默认值就行，然后重启这个时候我们就不填，两个都不填，看一看。他能不能采用默认值发送哦？还没有启动好，我重新发一遍，这回好了，我们看下他这控制台，发现他采用了我们默认值这默认的名字，然后这个是默认的年龄，我们再来给他加上一些发送，如果你有值的话，他就采用你的值了。

说话人 1 01:31:50
现在我们来看如何提取 body 参数，我们是用这个 request body 注解。但是注意的是 requestbody 这个注解，它是只能用于你自定义的类，像 Java 官方提供的 string， integer long 都是没法用于 requestbody 注解的，我们可以试一下。

说话人 1 01:32:15
假设我们就一定要用这个 string 试一下，看能不能提取，我们把这个 query 参数都删掉，我们再去 body， 记得选 json， 我们现在都是用 json 格式的。这里写一个 name， 我们点击发送，然后发现它。怎么成功了哦？我好像没有重启，我重启一下，我们点击发送。

说话人 1 01:33:21
他竟然，他是把我整个 json 都赋值给了 name， 那不用 string， 我们用，我们先不测这个，我们试一下这个，我们。我们现在试一下 age 可不可以？刚才那个 string 它其实是不对的，因为我们只想要这个小明，它就把整个都赋值给了这个 name， 这肯定不是我们想要的。

说话人 1 01:34:02
这个给人 age， 这个是18发送，发现这有个报错，说明它是没法用 request body 来接收我们的，像这个 string， int long 这些的。像这个 requestbody 这个注解，它叫接受，必须是我们自定义的类，例如这个就是一个正确的示范，这个 userprarm 是我自定义的一个接收参数的类，我们重启一下，这个时候我们在这里边写 name。

说话人 1 01:35:01
现在点发送，我们看一下是否能成功接收。name 是我们写的，然后 age 也是我们写的，这个就成功的把我们这个 body 里面的 name 和 age 然后绑定到了这个 userparam 里，这个 name 和 age 这个就是正是刚才我们在这个工具里边发送的请求。

说话人 1 01:35:30
现在我们来讲一下如何提取 path 参数。path 参数就是写在 URL 里这个两个斜线之间的参数，我们可以用 pathvariable 注解来提取。我们重启一下项目，我们现在不用 body 了，把 body 设为 null。我们也不用 params， 也不用 body， 也不用 header， 因为这个是直接写在这个 URL 里的，我们来试一下，假如说这个是789，然后发送看一下能不能取到，可以看到 ID 是789，这正是我们写的这个 hello， 这是 hello， 然后 ID， 然后这789就可以对应上它，又把这个789，然后给了这个 ID。这个 pathvariable 当然不是只能用一个，我们可以用多个 pathvariable， 你可以这个连着用，当然也是可以的，我们来看一下效果。

说话人 1 01:36:45
说这个是456，然后后边是789，我们发送看一下，ID1是456， ID 2是789，这也是刚好可以对得上的。这个是 hello， 然后这个是 ID 1456，然后 ID 2789，它把我们这个两个斜线之间的这一段，然后来提取出来，然后绑定到这个ID1上，然后把这个提取出来，绑定ID2上，这个就是直接嵌在这个 URL 中两个斜线之间的参数。可以用 pass variable 来提取这节课，我们来讲通用返回模板的包装器，这个需要我们用到之前写好的项目，我们打开浏览器，这个就是我们之前发给你们的这个代码仓库这个克隆点，克隆就有一个地址，把它复制一下，这显示已复制，就已经复制好了。我们回到 idea， 这有个文件新建来自版本控制的项目，这有让你写仓库 URL， 把这个粘进去。然后我们点击克隆，问你在哪里打开，我们就点击在新窗口打开，现在我们就有一个新的窗口，它现在是在同步，它是在导入 Maven 项目，现在它已经同步好了，就是 Maven 的那些依赖已经下载好了。

说话人 1 01:38:28
我现在来讲一下这些是干什么的吧？这些这个 SQL 文件夹里下放的就是这个 schema 点 SQL， 这个就是用来初始化数据库的，我这个是 MySQL 的这个初始化，然后 Maven 用的 POM 点 XML， 这不是有 MVC 的依赖，然后 Mybatis 依赖，然后这些这个是这个完整项目用的依赖，我们要用的包装器是在。这里我们把它复制一下，新建一个文件夹粘进来，有些东西它没有，因为 result 还没有 result， 我们再把这个 result 给粘进来。这个是在 VO 里面的，新建一个叫 VO 粘进来，这个就是我们一个返回的模板，我叫它是 commonresult， 对于前端的每个请求，我们都需要按这个模板来返回这个 success 就是表明这个请求是否成功。这个 code。就表明业务的状态码 message， 对业务状态码解释 data， 这个是如果是要有数据的话就在这个 data 里边写，没有的话就是一个 data， 就是 null。我们回到原来的 Controller， 我们把这些都删掉，就留一个 get mapping 就够了，这个也都不用。

说话人 1 01:40:55
我们假设先不启用它，我们看一下不启用它的效果，如果我们返回的是字符串的话，那么前端在请求的时候会收到什么方法？改成 get， 这个都不用。发现它返回的就是一个字符串，如果我们把它改成 void， 看看前端会收到什么，重启一下项目。

说话人 1 01:42:10
发现什么都没有，那如果我们返回一个 Hashmap map， 我们来试一下这又会是什么样的效果？

说话人 1 01:43:13
还没。返回了一个 json， 是 key 和 value， 那如果要给前端返回是一个 object， 我们再试一下新建一个，我就为了测试就写的 test， 加上自动生成 get 和 set 的注解，然后。我给它设置相应的值返回它，现在我们来看一下返回的这个自定义的 object 是什么效果。

说话人 1 01:44:35
发现它也是一个 JSON， 从刚才的实验看出，我们返回一个 string， 或者返回 void， 或者返回一个对象，它们的返回的前端的格式都不一样，那我们怎么样才能把它们统一一个格式呢？我们可以引入这个 common result wrapper， 把它启用一下这个 objectmapper 就是用来把对象转换成 json 的。这个 supports 方法就是判断是不是否要启用。我这个直接 return true 表示永远启用 before body write 方法就是写如何进行增强的。如果 body 是 null 的话，就直接返回一个 access 的通用的模板。如果是 string 的话，我就把它包装一下，然后返回如果它已经是这个 common result， 也就是已经是我们这个返回的模板的话，那我们就什么都不用动，直接返回就可以，我们来看一下效果是什么样的。诶，重启一下。

说话人 1 01:46:09
这时候我们可以看到他就把它包装了， success 是 true， 然后 code 是200， message 操作成功， data 在 data 里边才放了我们刚才写的数据，我们来可以测一下其他情况，也可不可以这样包装，我们试一下 string， return 字符串重新启动一下，看一下 string 能不能帮我们正常的包装，可以看到，也可以帮我们包装了一层，那么返回 void 呢？看看 void 能不能帮我们包装。

说话人 1 01:47:18
可以看到 Wade 也可以帮我们包装，当然这既然 null 他也给你显示出来了，说明他把 null 也序列化了，说明我们这个序列化的配置其实还没有调好，我们来到这个新的项目里面。这个 Jackson 就是用来配置 JSON 序列化的，我们把它粘过去到这个配置文件里面，这里边有一个叫做 default property inclusion， 这也就是只有不是 null 的字段它才会序列化。那么重启完以后看一看它 null 还会不会序列化，可以看到这个时候对于 null 的字段它就不会再序列化了。

说话人 1 01:48:15
这段配置建议直接照抄，我们通过这个类，也就是这个。包装器可以让我们 controller 就不用每次都 return 一个 commonresult 了。就像这样，假如说我们没有这个包装器的话，那我们每次都要这么写，就是每个方法都要写一个 return commonresult success， 然后这个方法也是 return commonresult success， 就有很多重复的代码，有这个包装器以后，我们就可以直接把这样代码删掉，比如这个。Wrapper 来帮我们包装，就按这个逻辑帮我们包装，这样在 Controller 的代码里就可以更加简洁。