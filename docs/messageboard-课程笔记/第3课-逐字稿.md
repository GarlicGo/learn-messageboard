会议主题：1-后端培训项目 留言板 讲解（3）-480P 标清-AVC
上传时间：2026年3月31日（周二） 02:21 （GMT+08）
智能纪要：1-后端培训项目 留言板 讲解（3）-480P 标清-AVC 2026年3月31日

说话人 1 00:00:01
这节课我们来讲一下如何利用 Mybatis 插入第一条数据。为了使用 Mybatis， 我们首先看一下 POM 点 XML 这个注释，我们不能再注释了，这个得取消注释，这俩一个是 Mybatis 依赖，然后一个是 Mysql 驱动的依赖，这两个的话我们肯定是得取消注释，然后把它引进来的。我们看一下，现在我们只有三条依赖，这显然不够。我们重新加载这个 Maven 变更，看一下这个依赖项，好了，现在有5条了，我们加载完变更以后发现这个 Mysql 和这个 Mybatis 都是刚刚突然多出来的，这是因为我们取消这个 POM 点 XML 注释之后，它就帮我们引进来了这个 Mybatis 和 Mysql 的依赖。现在我们把这个窗口关上，这还没有完，我们回到这个 application.YML 中。我们需要配置我们的数据源，我去把之前写好的项目粘一下这个 data source 整个，诶整个复制，然后粘进来，这个是在这个 spring application 里面的，诶这个缩进不对，这个 data source 我们看一下它这个是什么级别的？它是和 data source 级别，和这个 application 这个级别和 Jackson 这个级别它是一样的。要顶到这来。

说话人 1 00:01:43
然后这个 driver class name， 然后 user name password URL 是 data source， 它的下层我们看一下另一个项目，可以看到这个 application， 然后 data source， 然后 Jackson， 它是平级的，我们需要保持这个级别，要按照这样的格式来对齐这个 user， name 和 password 一定要改成你们自己数据库的那个连接的用户名和密码，你们数据库的用户名和密码是什么？这个就怎么填。

说话人 1 00:02:14
这个就不要照抄，这一部分是你的数据库的名字，就是这个部分 message 布尔这个名字，这一部分和这个部分是要求一样的，所以你们要改的三个地方，一个是用户名要改，一个是密码要改，还有一个是这个数据库连接的这个数据库名字要改。现在我们把这个数据源加到这个 application yml 里后，我们去找到这个 Mapper 文件夹，记住是这个 src main， 然后 Java 里边这个 Mapper 文件夹点开。然后给每一个 Mapper 都加上 Mapper 注解，这个是每一个都要加 Mapper， 加上 Mapper， 加上这个也是 Mapper， 加上你可以看到加完 Mapper 以后这多了一个绿色的图标，因为加上 Mapper 以后它就可以被 spring 管理了，如果你不加的话，它就没法被 spring 管理的。现在我们看一下，4个确实都加上了。现在我们来开始写第一条 SQL 语句，我们以 user 表为例， user service 首先在这里加一个接口 insert， 这个是 string。

说话人 1 00:04:15
光写完接口还不行，因为我们这个对应的 XML 文件里边是没有任何东西的。不过我们先从机构开始讲这个接口里边，这是返回值，这个是方法名，这个 param 注解是一会你在 XML 文件中使用的名字，这个 username 就是我们之前的 Java 这个方法中的名字。

说话人 1 00:04:42
param 注解，这个是如果你不是传一个类型的话，就是如果你不是像传这种 user 实体类或者是 message 这个整个类传下来的话。你要是传 string long int 这种类型的话，这肯定是要加一个 param 注解，这样表示一会你在 XML 文件中怎么使用你这个参数名？怎么使用这个参数？一般情况下这个 param 里面这个字符串和这个方法名字都是一样的，我们看一下相关问题。

说话人 1 00:05:17
哦，应该是把我加错地方了，应该是加到这个 userMapper 里面，我刚才粘错地方了，点击一下这个，它有个标红的地方放上去，这上面说 statement， 然后 with ID insert not defined in Mapper XML， 就是意思就是说你这个 Mapper 接口里边有这个方法，但是你这个 XXML 文，那个 XML 文件里面没有，我们点一下这个 generate statement， 它就帮我们生成了。

说话人 1 00:05:52
我们这里 insert into t user name password values user name password 这个用井号大括号引起来的，就是我们之前在 user map 定义的这个字符。这个 username 相当于这个 username， 这个 password 相当于这个 password， 现在我们来测试一下是否可以使用，我们用测试类在这里边 autowire 一下，我们这次。auto wire 下是 user Mapper， 这有一个图标可以导航到自动装配的依赖项，点一下可以它发现它成功的导航到了这里，这正是我们想要的 user Mapper。

说话人 1 00:07:24
我们可以使用 test 注解来标注一个测试方法，用按 test 这个名字可以随便取。insert 我们来运行一下，看一下效果。在运行之前，我们先点开这个数据库，双击 t user 看一看里边有数据吗？但是一条数据都没有。我们点击这个绿色的按钮，然后点击运行，正在编译，稍等一会。

说话人 1 00:08:43
诶，看什么回事？不好意思，我忘了。还有一个在这里面，这个 Mybatis 的这个 Mapper locations 我忘写了，我们回到这个 application， 点 yml 加上一条，这个是 Mybatis 的 Mapper， 这个 locations 你放在哪里？我是放在这个 resources 文件夹下的 Mapper 里面，所以这边就写 Mapper 里的所有的 XML 文件都是。

说话人 1 00:09:31
然后我们看一下，诶，重新运行一下，诶？我看看还是哪的问题。

说话人 1 00:09:57
unknown column ' paas。

说话人 1 00:09:59
哦，懂了，我这个看一下这 user mapper 点 XML。他这个 password 写错了，我们改正以后我们再来一遍，现在我们看一下他的测试已通过，这都是绿色的一个对勾，我们来到数据库里面就可以看到 user name 是我们刚才填充的 password 是我们刚才填充的 user ID， 采用主键自增，它帮我们自动生成 nickname、 sex、 user role 均采用默认值。

说话人 1 00:10:45
create time 和 update time 是由 Mysql 数据库帮我们自动填充的，我们没有写，我们传进去的是 null， 我们说我们可以说根本就没有传，它可以帮我们自动填成更新时间和创建时间，这样的话我们第一次 insert 插入就完成了。

说话人 1 00:11:04
讲完了 insert， 我们现在来看一下如何 select 我们到实体类这。像这个 password， 我们可以在实体类上改成 password， 然后 user row 可以改成 row。然后我们去在这个 Mapper 接口，在这里来 select 一下返回值，当然是 user 导入一下传进去的参数。

说话人 1 00:11:59
这个时候点击 generate 等待，可以看到这个井号和大括号里边的值，点击一下这个跳转和这个 harem 里面的值是一样的，如果我们不写这个，改成其他的。我们再到那边可以看到它这个代码提示，就是和我们这个 param 注解里面的字符串是一样的，我们改回 userid， 我们。到测试类里面测试一下，使用 getbyid， 然后我们打印出它，我们看一下这是什么效果运行。

说话人 1 00:13:49
看一下它和数据库里是不是一样的，我们点开数据库，打开这个 user ID 是 null， user name 可以， password 是 null， nickname 可以， sex 0可以， role 是 null， 然后 create time 是 null， 然后 update time 是 null。为什么有的它就可以转化到实体这个类里面？有的它就没有，为什么就是 null？这因为我们这个它的名字它不一样的，像这个。

说话人 1 00:14:23
password， 这个 p as， 然后 WD， 然后这个是 password， 因为这个数据库的这个字段名，点开这个列，看到这些字段名和我们这个类名，点开这个 user， 发现它是不一样的，导致它这个数据库里查出来以后没法绑定到我们这个类上面，导致一些字段就出来了。

说话人 1 00:14:46
这怎么解决呢？我们可以来到那个项目里面，我们看到另一个项目怎么解决的？打开 Mapper， user Mapper 看一下这个有个叫做 result map 的东西，我们把它复制过去，然后我们来到我们这个项目的 userMapper XML 里面一粘 result map 就是结果映射的意思。这个 ID 就是给这个 result map 起个名字，然后这个 type 就是指定你的 Java 的那个类在哪里，这个我写的是全类名，前面就是从 club， 然后 dev Hub message board 开始写，后边一直写到你这个类名。我们发现这个有的是 ID， 有的是 result， 这个 ID 就是应用于主键的，而这个 result 就是应用于非主键的 property， 应用于我们这个实体类，我们点进去这个实体类给 user ID。

说话人 1 00:15:52
miao user ID， 然后 column 是只有我们数据库的名，我们打开数据库看一下这个 user ID， 然后这个 user name 是我们实体类的 column 是数据库的，例如这个 password， 像这个 password 我们实体类是写全了，是8个字母，然后这个数据库只有 Paas， 然后 WD 六个字母，还有这个 row， 我们这个实体类是只有 row 一个单词，但是我们数据库里字段名是 user row， 然后这有8个字母，还有一个下划线。这就导致了我们这个实体类和数据库它们的名字不一样，导致它查出来的话就查不出来，就是 null。

说话人 1 00:16:36
我们可以通过 result map， 然后来把它做一个映射，但是这样的话我们这个 result type 就不要写 result type 了，我们改成 result map， 可以看到这个 result map 这里写的是这个 result map 的 ID， 这俩是一样的，这叫 user result map， 这个是 user result map， 就是你定义一个 result map， 要给它起个名，然后你在这里 select 的时候， result map 这个名字就写你这个 result map 的名字。

说话人 1 00:17:09
我们现在改成这个 result map 以后，我们再试一下，我们之前试完以后出现这么多 NULL， 我们看看现在还会不会出现这么多 NULL？我们来看一下 select 出来结果，我们点开这个 user 表里 user ID 有， user name 有 password 有。nickname 有 sex， user， role 也都有。包括 create time、 update time， 这也都有说明这个数据库里这些字段的这些信息都已经选出来，并且写到我们这个 user 这个类里面了。所以这个 RML 里面这个 result map 是非常有用的，它解决了我们这个实体类和数据库它这个名字不一样的问题，我们增改、删查一定要熟练编写。

说话人 1 00:18:11
我们现在已经讲了如何去 insert， 然后也讲了如何去 select， 现在我们还没有讲如何去 update 和如何去 delete。我们现在就来讲一下如何去 update， 还有去如何去 delete。我在这里写一下 update， 我们想要根据他的用户 ID 去更新他的昵称。以及他。他的性别，我们还想让他删除，按照 ID 进行删除。

说话人 1 00:19:35
我们因为有 Mybatis x 插件，所以看这有小鸟，这小鸟你一点就能跳转到我们这个对应的这个 XML 文件里边去，然后你再一点，然后它又可以跳转回来，我们到这个我们的 usermapper 里面，这跳转来，这样就非常的方便。所以这个 Mybatis x 这个插件是一定要装一下，它现在报错说 statement， 然后 with ID update by ID not defined in matter XML， 就我们还没有在 XML 里面写这条语句，我们点击一下 generate。诶，怎么使用在这个位置？来，我先把它写到下面，然后这个我们也来一下 generate with， 问你选是哪种方法，我就选 delete， 怎么还在这里？来，放到下面，我们先写 update key user。get nick name。

说话人 1 00:20:44
等于还有自动提示，这个自动提示的内容就是这个井号大括号里自动提示的内容是和你这个 param 里的注解的内容是一样的。text 等于井号大括号， text bear user ID。等于 user ID， 来一下 delete where user ID 等于 user ID， 我们来格式化一下这个 idea， 快捷键就是 control， 然后 Alt l 可以格式化，格式化以后可以帮我们把这个代码变得更加整洁，现在我们这个 update 也写好了，然后 delete 写好了。

说话人 1 00:21:53
我们没有设它的返回值，我没有设它的 result type， 那我们还写 int， 因为就算我们没有设它的返回值是 int， 它默认就会返回 int， 这 update 就会返回你这个 where 条件它匹配的行数，然后你这个 DELETE 就会返回这个 where 条件它匹配的行数。你也可以换句话说，它 DELETE 返回的就是你真正删了几行它就返回多少。所以虽然我们这个没有写 result type， 是这个 int， 我们完全可以用这个 int。

说话人 1 00:22:29
来接收返回值，我们来到测试类里，我们来更新一下 update by ID 首先我们要传递一下 ID， 然后我们想给它设一个新的昵称，我们还得设一个新的性别，这是有代码提示的，像这个 user ID 是第一个参数，然后 nickname 是第二个参数，然后 sex 是第三个参数，我们按照这个顺序来写。性别给他设置成一，我们这时候再去把它删掉，不对，先不能删了，就没了，我们先更新一下运行，在更新前它 select 出来的是。未设置昵称 sex 等于0，我们现在去看一眼它现在是多少？这是 nickname， 这是新的昵称，这是 sex， 是我们先设置的性别，我们再试下 delete。

说话人 1 00:24:17
先试一下返回值，看一下返回值是多少，它这个 update 返回值就是它这个 where 条件，它匹配了几行，它就给你返回几行。

说话人 1 00:25:00
它的 update 返回值是一，正是因为我们这个 update by ID， 我们点开它这个有 where user ID 等于一，我们恰好只有一个 user ID 为一的用户，所以它返回的就是匹配的只有一行。我们现在讲完 update， 我们把它注释掉，我们讲一下 delete， 我们这个时候需要填一个参数，我们讲你就先不填一，我们填2，看看它返回值是多少。

说话人 1 00:25:54
那。来运行一下，那 DELETE 它返回值是0，它 DELETE 匹配，返回匹配的行数是0，因为我们这个 delete by ID， 它的是按照 where user ID 等于2去匹配的，但是我们这个表里并没有 user ID 为2的人，所以它一行都没有匹配到，所以返回的数就是0，我们现在把它改成一试一下。

说话人 1 00:26:33
因为刚才没有匹配到，所以什么都没删，我们可以看一下这个 user ID 等于一的用户还在这里，我们看一下返回值是多少？delete 的返回值它是一，说明有一行匹配，还真把这一行给删了。我们到 user 这里，这有个刷新按钮，我们点一下刷新。发现就啥都没有了。我们可以点这个加号给他添加一行，这个 default 代表有默认值，这后面5个全是 default， 说明这5个全有默认值，这个 generated 说明它是可以自动生成的。

说话人 1 00:27:25
所以实际上我们要填的也就这俩，一个是 username， 一个是 password， 我们把这俩一填，这还没完，我们得提交这个绿色的向上箭头，我们点一下这个提交，如果不提交的话，看你做的更改是不会写到数据库里面的。我们这点击给提交，然后它又写到数据库里面了，这个 user ID 采用自增生成的主键，我们不用写，它会帮我们生成。nickname 和 sex 用户 role 采用默认值 create time Update time 由数据库自动填充。我们看到刚才虽然可以选出来，但在控制台它不打印 SQL 语句，这对我们调试带来一些困难，我们能让它把 SQL 语句也显示出来吗？我们来到旧的项目里面，这有一个 logging， 我们把这行，把这三行来到我们这个 application 点 yml 文件里面。易粘 login。

说话人 1 00:28:30
是控制日志的 level， 是日志等级，我们把 club Hive message board map 这个包，其实就是这个包，这个包就是所说的 club Hive message board map 这个包把它日志级别改成 debug， 改成 debug 以后我们就可以看到它在控制台输出的 SQL 的日志了，就可以看到它是如何执行这个 SQL 的，我们可以试一下，我们点击运行。

说话人 1 00:29:30
我把这个窗往上拉一些，点到这，我们看一下这个箭头， select share from t user where userid 等于问号，这恰好就是。来，往下一点，这恰好就是我们这条 get by ID 的 SQL 语句，我们继续往下看，这有一个箭头， update t user set nickname sex 等于 where userid 等于，这恰好就是我们这个 update by ID 的 SQL 语句。他也打印出来了，我们再往下看这个箭头。

说话人 1 00:30:16
delete from t user where userid 等于正好是我们这个 delete by ID 的这个 SQL 语句，我们通过刚才这样设置，把这个 login 加一行，把它这个包下面的这个日记变成 debug， 我们就可以让它打出日志，这可以让我们在开发的时候调试更加方便。

说话人 1 00:30:37
我们来讲一下如何通过 HTTP 请求来操作数据库。我们首先需要创建 Controller 这个包，我们新建一个软件包叫做 Controller。然后我们在这个包里面新建一个类，要做 usercontroller， 因为这个类它这个 Controller 我们要加上注解，我们是前后端分离的项目，我们加的是 restcontroller， 注解为让它能打印日志，我们就加一个SLF4J的注解，我们在类上加的 requestmapping 可以作为这里面类里面的所有方法的前缀。我们这么写就是 API message board， 然后V1这个就当做我们这个 Controller 里所有方法的前缀里边的方法，像例如 get Mapping、 post Mapping， 它都是会拼接到这个类上面的 request Mapping 这个字符串。

说话人 1 00:31:49
后面我们来看一下接口文档，用户注册。一个 users register， 我们要 users， 这个 user controller 里所有都是以 users 开头，所以我们完全可以把这个 users 也提到这个类上面的 request mapping 上面。

说话人 1 00:32:19
注册，我们在这里边写 register， 我们看一下接口文档上它要求有 username 和 password， 这是 body 参数。我们在这里面新建一个包叫做 param， 就是专门存放 body 参数的，这个叫做 registerparam， 就是注册用的参数。先给这样一个 adddata 注解，让它帮我们生成 getter 和 setter， 里面需要有用户名还有密码，这样我们这个 registerparam 就写好了，我们回到之前的这个 control 里面。我们需要用 requestbody 注解，这个是 registerfrom， 从我们的 HTTP request body 里面提取出我们的 username 和 password。

说话人 1 00:33:42
我们 Controller 是需要。service 的，所以我们在 outware 一个 user service， 这是一个接口，我们可以点进去看，这是一个 INTERFACE， 它是 user in， 这个 user service 是一个接口，它装配的，但接口肯定没法装配，它装配的是实现这个接口的类，我们导航到自动装配的依赖项，发现它装配的是这个 user service impl。它的是一个实现类，这是 service 的实现，而 service 又是需要装配一个 Mapper， 这样我们看理一下它们之间的关系， Controller 依赖于 service， 然后 service 又依赖于 Mapper， 于是它们这样就有一个依赖的关系，我们需要 user service 调用 register 方法。

说话人 1 00:34:51
我们这里边需要提出它的 username， 还有参数里边提出它的 password， 我们鼠标点一下，我们没有必要去这里面再写一个方法，这个 idea 它有一个很方便的工具，你鼠标点一下，然后 Alt enter， 然后再 enter， 它直接帮我们生成这个方法就非常的方便，我们点一下这个向下箭头点一下鼠标放上去，它说这个类必须声明为抽象，要么实现它的抽象方法，我们点一下实现方法，点击确定它直接帮我们生成这个方法，我们都不用写什么东西。

说话人 1 00:35:31
包括这个接口，这个方法，还有包括我们这个方法都帮我们生成好了，我们这里面来插入一条 userMapper， 点 insert、 user name 还有 password， 这样我们就最简单的一个接口就写完了，当然这不符合我们要求，因为我们这只是一个示例而已，我们当他请求的时候，他会调用这个 API message board v users， 然后 register 发送的是 post 请求，它在 body 参数里面会携带 user name 和 password。

说话人 1 00:36:12
通过这个 requestbody 注解提出来，通过 userservice 的 register 方法看一下它的实现，然后这个 register 方法会调用 usermapper 的 insert 方法把它插入到数据库里，这样一个新用户就注册完成了。我们运行一下，看一下可不可以实现这个效果。

说话人 1 00:36:50
看出区别，我们先看一下这个 user 里边有什么，发现 user 里边就只有 ID， user ID 为2的一条，我们去接口测试工具，这个环境我们得改一下，因为我们是在里边加了一个 API， 然后 message board， 然后V1这个环境，这个前缀 URL 就是我们这个 apifox 的 apifox 它的前缀我们点击保存，我们给它关掉，这有一个运行。

说话人 1 00:37:25
我们点一下这个运行，我们点一下自动生成，就用它自动生成的，我就不改了，我们点击发送，我们看下返回的是什么？返回的是 true， 然后 code 是200，然后 message 操作成功，这正是我们想要的，我们去数据库里面看一下，我们刷新一下，看一下，诶，有密码诶，也有昵称、性别，还有用户的角色，都是默认值。这正好是我们机构测试工具给。传来的数据，我们来多插几条发送龙霞。好，现在这个用户也插入了，再生成胡静，好，再发送，再生成田静，再发送。又成功了，我们再去数据库里看一下，刷新一下，哎，现在变5行了。

说话人 1 00:38:27
这三行正是我们刚刚插入的这个 userid， 我们设置了数据库的自增，所以我们不用管 userid， 这个 userid 是数据库帮我们生成的。于是我们就完成了用接口测试工具来往数据库里插入一条新用户。我们之前在接口文档里面定义了一些常量，我们写到了这个对照表里面，像用户性别有012，代表未知男女，还有留言类别有01234，例如学习、生活、娱乐其他。

说话人 1 00:38:59
那这个我们怎么写到 Java 代码里边呢？其实 Java 里边有一个类叫类型，叫做枚举，我们把这个类型都会放到这个项目的 content 包里面。我们先把它停一下，在这里面我们新建一个类，新建一个包。说错了，我们新建一个软件包叫做 constant， constant 呢？就是常量的意思，里边我们就放一些枚举，我们回到示范项目里边，这些像 messagecategoryenum， 还有这个举报种类。返回的业务状态码，这些都是枚举，它是用这个 enum 定义的，我现在就用 user sex 这个 enum 来示范一下怎么创建枚举，我们在这个鼠标右键新建类，我们往下看这个第四个就是枚举，我们把名字粘过来，然后按 enter， 它帮我们创建了枚举，它比较有些像这个类 class， 那么有些不同的地方。

说话人 1 00:40:22
我们看一下这个文档是怎么说的，它用户性别有三种，零未知、一男二女。那我们也这写下面枚举的命名规范是里面枚举一律用大写字母，中间用下划线隔开，未知就是 unknown， 然后括号里边写的是0，还有男 male 括号里边写的是1。然后女 female 2里边写的是2，你写完了以后，最后用分号结尾就没写完之前这些都是用逗号，写完之后就是用分号，我们需要生成一个，我们需要是有一个 private good， 这是枚举里面的一个字段，我们生成一下它的构造函数，现在。就基本写完了，但是它有个标黄，看它说什么，他说还没有访问，我们再给他生成一下 getter， 生成 getter 点击确定，这样我们这个枚举就写完了，我们看一下和我们之前这个有什么区别。

说话人 1 00:41:56
unknown male female private final int。

说话人 1 00:42:06
可以看到它这个是可以设为 final， 因为你一旦赋值以后你就不会改了，所以你设为 final 是可以的，这样的话就不用担心你的枚举会被更改里面的值。而且我们也本身也没有提供 setter 来给它让它更改，我们把其他几个枚举也粘进来。按住 Ctrl 全选，然后把它都粘进来，粘到这个包里面。

说话人 1 00:42:47
我来讲一下这个 consan 这些里的这个枚举都是些什么吧？这个是 user role， 这个 authorities 它标红，不用管它，因为这个是我们 spring security 里的工具类，这个 result code 这个是用于我们的业务状态码，包括参数校验失败，然后用户名已存在用户名或密码错误重复操作。

说话人 1 00:43:13
这个是。举报类型，你可以举报留言，你可以举报评论，这是留言种类，这个是比较特殊的，因为你发现它括号里边其他的枚举，它括号里边都只有一个数字，而这个 result code in out， 它括号里边竟然既有一个数字，也有一个字符串，这当然也是可以的，我们看一个枚举里面也没有规定一个枚举里面只能有一个 int 型字段。对，这个是可以用 int， 有没有必要用 int 改成 int？

说话人 1 00:43:54
我们回到这个里面，回到，诶，你一个枚举里面完全可以有 int， 也可以有 string， 甚至有更多都没，都可以，这是没有限制的，这个命名规范还要注意一下，就是全大写中间用下划线隔开，这个是里面的保存的字段，因为它不会更改，我们都定义成 private final。

说话人 1 00:44:25
这个是它的构造函数，这个就是 get code 和 get message 可以通过这个方法来获取这个枚举里面的值，我们可以看一下如何获取里面的值，我们可以打印一下。首先我们用这个 resultcode enum， 然后点可以看到，点就可以看到有这么多枚举类型，假如说这个给按 enter， 然后再点可以看到这些 get code， get message 都是这里面的。get code 或者 get message， 我们来 get message 试一下。我们来运行一下，看一下什么效果哦。

说话人 1 00:45:09
这个先把它注释掉，我们来运行一下，看一下它打印出来是什么，看应该它打印出来的应该就是这个 user name already exist message， 用户名已存在，就相当于这里面的用户名已存在。我们这个简单的注册接口已经写完了，但是这个接口还有一些问题，就是如果用户瞎填一些内容，这个接口也是会接受的。我们看一下如果用户瞎填会怎么样？我们找到启动类把它跑起来，现在项目已经起来了，我们现在来到这里，我们点击发送，他发现有个操作成功，关键是我们这里边都是空的，他竟然还能操作成功，我们来去看一下数据库。

说话人 1 00:46:16
打开这个 user， 这里边 user name 是空的， password 也是空的，这竟然都能提交成功，我们再来，我们甚至连这个都不写了，看一下发送，嗯，这时候500看一下，这个时候数据库里 insert 的时候出现了数据库的一个 not null 的一个异常，这是。违背了他的 not null 约束，所以说明我们设置数据库 not null 的约束还是很有用的。为了防止用户瞎写，或者甚至用户可能会给你写好长一段像这个名字，我们来写，很长很长，我们再发送，这次看看他又说了什么。

说话人 1 00:47:23
data too long 这个 user name， 它这个要插入的东西太长了。我们数据库一共也才给 user name 设置了 VARCHAR 20，他一下插了超过20的数据，他这个数据库在插入的时候就会开始报错，那有没有办法？就是能不能先校验一下用户的输入，不要让他乱输入，这就是说明我们要进行入参校验，要开启这个功能，我们先去 POM 点 XML 里面需要引入，诶，我先去那个项目里边看一眼，看到示范项目的 XML 找一下。

说话人 1 00:48:07
参数校验，对，把这个 dependency 复制一下，我们粘进来，粘到这个 POM 点 XML 里面，我先把它停掉，我们在重新加载 Maven 变更之前，发现它只有5个依赖，我们重新加载一下这个 Maven 变更多了一个叫做 spring boot starter validation， 我们把这个 validation 的依赖引进来以后，我们就可以进行它的入参校验了，它的方法也很简单。

说话人 1 00:48:46
我们找到我们这个 param， 首先我们不能是空的， not blank 和 not null 它们是有区别的，像 not null 它只是要求不要求为 null， 但是可以是空字符串，而 not blank 的要求更高，它不仅不能是 null， 甚至连空字符串都不可以，是必须得有东西，像这个 password 是用 not blank 修饰一下。

说话人 1 00:49:15
我们看一下数据库里是怎么限制的？数据库里要求 username 的长度20，最多只能是20。这个 password 数据库里没有要求 password 是多少，因为 password 不管你多长，它加密完以后它都是定长的，但是为了防止用户名输入的 password 过长，我决定还是限制一下这个密码的长度，最大也只能是60。这 length 最大只能是20。

说话人 1 00:49:56
not blank 和 not length 可以是可以同时用的，就是说这个 param 里边一个字段上加的参数校验注解可以不止一个，它们两个会同时生效。我们加完注解以后，我们回到我们的 Controller 里面，要在这个方法上面加上一个 advalid， 我们再启动项目，看一下这回加上参数校验以后有什么效果。

说话人 1 00:50:25
我们参数校验其实非常简单，无非是在这个 param 里面。在这个字段上加了一些约束，然后是在 Controller 里面这个方法前面加上一个。注解而已，其他的我们还都不用做，就这么简单就开启了参数校验功能，我们这时候再故意不填，我们上次故意不填，它是插入成功了的，我们看看这次。给他一个空字符串会怎么样？发现他有一个 method argument not valid exception， 说明参数校验失败， author 不能为空，然后 user name 不能为空，那他进入 Controller 里面了吗？我们可以打印日志看一下，如果他进入了 Controller 里面，那么就会有条 info 打印出进入了 Controller。

说话人 1 00:51:45
我们发送发现并没有这个 info， 说进入了 Controller， 说明它根本就没有进入这个 Controller 方法就被拦住了，在这个参数校验的时候，它就没有经过参数校验失败，导致抛出异常，于是就成了我们现在看到的这样，但是这个控制台打印的这些有些过于令人看不懂，就是过于繁杂，我们看一下。它这个返回的格式， time stamp status error pass， 这也不符合我们要求的这个返回模板有什么办法让它符合我们返回的模板呢？就是相当于我们之前定义了一套返回的模板， success code 这些。这就到我们的全局异常处理器，我们到打开示范项目这个 global exception handler， 把它复制一下，我们粘进来，还有些 spring security 相关的，我们先把它注释掉，现在就可以不报错了，我们来重启一下项目。

说话人 1 00:53:43
我们再故意发送空字符串，发现它符合我们的返回模板，这都是全局异常处理器的功劳，我们看一下控制台打印出来的也很好，懂了这个是哪一个请求？是什么类型的异常？还有异常的原因是什么？既然全局处异常处理器这么厉害，我们看一下它里边写了些什么？这就是全局异常处理器，它上面先用 restcontrolleradvice 来标注，这是让它注入到。

说话人 1 00:54:21
润容器里面 exceptional handler 说明标注这个方法能够处理哪种类型的异常。这个里边写的是异常的 class 对象，然后这个方法我们写的是这个异常会传进来我们参数校验失败的异常，就是这个，我们把这个异常通过 Formatter by exception 先把它格式化了一遍，我们看一下控制台， password 等于不能为空， user name 等于不能为空，这也正是我们格式化的结果。可以看到这个等于还有方括号和我们后和控制台打印出来的确实是一样的。然后我们格式化以后，我们用 VAR 级别在控制台打印，并且返回了一个 common result。我们用 VAR 级别的控制台打印，并且返回了 common result， 也就是这种我们说的返回模板，包括 success code message， 然后我们到这里面看，就发现后端返回的符合我们的通用返回的格式了。

说话人 1 00:55:46
但是我们打印出来的这些不能为空，这也不能为空，有没有更好的让人更加能理解的方式呢？我们可以来到这个 param 包里面的这个 registerparam， 我们可以通过加上 message 的方法来指明更详细的信息，比如用户名不能为空，这里指明。

说话人 1 00:56:20
message。用户名长度最大是20，这里指明这个是密码，不能为空，这里面是 message， 这是密码长度。诶，密码长度应该最大是20，我刚才写错了，密码长度最大是20，我们再来看一下。我们写完 message 以后它返。回的是什么样的信息？

说话人 1 00:57:15
它应该就不再是这种不能为空这么简略的信息了，我们点发送，发现它是密码不能为空，用户名不能为空，它返回的正是我们刚才写的这种在。它让我们这个类里面的 message 信息，那我写几个超长的参数，看一看它能不能也显示出来。这个就超故意超过20个字符，这个也故意超过20个字符，我们点击发送可以看到它显示的是用户名长度最大是20，密码长度最大是20，这也是我们写的 message。说明我们可以通过 message 方法可以更加详细的指定是什么样的错误信息。

说话人 1 00:58:15
通过我们在这个 param 类上面加 not blank， not AAA， not blank 或者 at length 注解，可以再在 Controller 上面通过 ad valid 注解启用参数校验。然后我们别忘了在这个全聚像处理器里面捕获这个异常，并且把它打控制台打印。并且用 Commonresult 包装起来，这样前端返回的就是一个符合我们通用返回模板的请求了。

说话人 1 00:58:47
其实我们不只有 not blank 和 lens 这么点儿注解，还有很多注解，例如 min， 这是要求最小不能是小于多少，还有 Max， 这是最大不能超过多少。还有 not null， 这个是不能为 null。我们去这个包里面看一下一共有哪些 a third false， a third true， 然后 digits Email future， 像 future 就是指定一个未来的时间， Email 指定是一个必须是个邮箱的格式， Max 最大不能超过多少。最小是多少？not blank 不仅不能为 null， 而且还不能是空字符串，或者也不能是全是空格。not null 不能为 null， null 必须为 null size 指定它的这个大小。还有 len， Len， len 是在这里面。

说话人 1 01:00:00
长度。长度呢。在这里包括长度，我们也可以指定它这个字符串的长度里边又可以细分为长度里面还可以写 Max， 也可以写 min， 可以既写 Max， 也可以写 min， 像这样写的话，就是最大是20，最小是10。这样我们就可以很优雅的完成我们的参数，我们在项目中可能会遇到各种异常，需要我们自定义一些异常，我们自定义异常都是放在这个 exception 包下面的。

说话人 1 01:00:48
我们首先在这里面创建一个包叫做 exception， 这个包里面专门放异常，我们要新建异常就是很简单，我们现一个类叫做假如说我们遇到一个请求资源不存在的一个。一个请求，我们就可以抛出 notfuncexception， 这个异常完全是自定义的，也没有人规定说一定要起这个名字，这些属于自定义异常，我们自定义异常，自定义的异常通常是叫做继承 runtimeexception 类，这个是继承自运行时异常，它就可以直接抛出，而不用 trycatch 或者是先声明才可以。因为 runtimeexception 下面的子类都是非检查型异常，我们生成一下它的这个构造函数，这样我们一个异常就写完了。这里面的它的构造器要求里面提供一个 string， 它会调用它的超类的构造器。我们看一下示范项目里面还有两个异常，我们把它复制过来粘进来。这个是非法操作的异常，抛出这个异常，它会返回 HTTP 状态码403。而我们这个 not found exception 是一个请求资源不存在的异常，抛出这个异常它会返回 HTTP 状态码404，然后这个业务异常，这个异常应该是我们定义过了最复杂的异常了，它接收的构造函数里边要求盘传进来一个是 result code enum 的一个枚举，我们看这个枚举有什么东西？这里面这个枚举里面有一个 code， 有一个 message code， 就是我们接口文档上定义好的一些状态码。包括10001创建失败，10002用户名已存在，10003用户名或密码错误，1004重复操作。

说话人 1 01:03:03
这个和我们的状，这个接口文档上的对照表是一样的，我们回到这个业务异常里面，它这个构造函数里面传进来一个业务状态码，你也可以补充一个，传进来一个信息，如果你不写这个信息的话，那它就默认使用这个 result code inum 里面的 message， 如果你写信息的话，它就会使用你写进来的信息。我们既然定义了异常，那么抛出去如果不在全局异常处理器里声明的话，那也是没有什么用，没有什么意义。我们来到全局异常处理器，我们抛出异常是肯定要在这里面捕获的，如果我们不在全局异常处理器捕获的话，那么它返回的格式就不符合我们的这个通用返回模板的格式，它就不会遵循我们这种 success code message 这种格式。所以我们需要。

说话人 1 01:04:06
在这里把它捕获之前，我们注释掉了，我们这回不注释，我们可以看到这个叫做 response status 的一个注解，这个注解其实就是设置我们给前端的 HTTP 的状态码是怎样的，如果你不设置，默认是二版，例如这个吧。我们这个没有设置这个 response status， 它就返回的是200。像这种我们设置了 response status， 那么它就按照你指定的状态码这个 HTTP status， 它是一个枚举，我们看一下 method not allowed 对应的是多少，看到它对应的是405，我们使用它的这个 a， 这个是 value， 我们可以用 getvalue 方法提取出它这个 value 值就是405。业务请求格式不对的话，我们返回的状态码是。badrequest 看一下是400。

说话人 1 01:05:22
如果我们捕获到了这个 illegaloperationexception， 那么返回的是 HTTPSTATUS 403如果我们捕获到了请求资源不存在的异常，也就是我们刚才自定义的异常 notfoundexception， 那它返回的 HTTP 的状态码是 NOTFOUND 404。如果我们捕获到了业务异常，那么返回的状态码 HTTP 层面的状态码是2，因为我们没有指定。response status 如果没有只能 response status 的话，它就返回200。

说话人 1 01:06:03
还有最后一个是 exception 的 class， 这个是如果前面的异常都没有被捕获的话，会被它捕获，它返回的是 internal Server error， 它返回的是500。我们来做个实验，我们在这个 Controller 里面抛出异常，试一下，我们访问它的时候。我们可以抛出。妞。我们先试 not found exception 吧。

说话人 1 01:07:25
它返回的是请求资源，不存在，我们关注一下它这个 HTTP 状态码，它返回的是404，我们换一个，我们换成 illegal operation， 试一下，我们再发送。这个时候我们看一下它返回的状态码，它是403 forbidden， 这正好是我们在全局异常处理器里设置的 response status， 是403。我们再换一个，这回我们写 business exception， 然后里边传进去一个 resultcode in 呐？假如说这个我们看一下，点发送。这个时候它返回的状态码是200 ok， 但是它是在这个请求的 body 体里面指明了 code 是10003，用户名和密码错误，这和我们的状态码对照表是符合的。

说话人 1 01:09:07
如果抛出业务异常的话，我们是返回 HTTP， 状态码是200，但是会在这个返回的返回模板里的 code 和 message 里边写，这是为什么？出错了？如果你们有需要的话，也可以定义更多的异常，都是在这个 exception 这个包里面定义更多的异常，记得去全局异常处理器里面把它捕获，如果需要返回不同于200的状态码的话。

说话人 1 01:09:36
需要用 response status 里面传递一个 HTTP status， 用 exception handler 里边标明你想捕获哪种类型的异常，然后这方法里面写如何处理异常返回的返回的是一个 common result， 这样让它返回符合我们的通用返回模板。

说话人 1 01:10:00
这节课我们来讲一下数据库的事务，一个数据库的事务要遵守 acid 四个特性，它们是原子性、一致性、隔离性和持久性。原子性就是指一个事务中所有操作要么全部完成，要么全部不完成，不会在中间某个环节结束，就像这样，要么全做完，要么全不做。我们来到项目里面看一下 spring 是怎么实现事务的，这是 Controller 里面的 register 方法，调用了 service register 方法，我们这里边往里面插入三条数据。我们来启动一下，我们发送，在没有抛出异常的情况下，一切都很完美。

说话人 1 01:10:57
我们看一下这个 user， 发现有三条数据是没有问题的，我们先把它删掉。那假如我们中间抛出了异常会怎么样？那我们就随便抛一个异常，就像这个随便抛一个。不可到达的语句。这样，用这个吧。会抛出一个除0的异常，我们启动一下项目，随机生成一个值发送，我们来看一下，这时候数据库里会有数据吗？会发现它有两条数据，它这条成功插入了，这条也成功插入了，在这个时候抛出异常，并没有导致前面插入两条数据回滚，就在数据库里产生了脏数据，诶，点错了，这两条数据我们不想看到，怎么让它回滚呢？就是一旦抛出异常，整个这个方法里的操作都会被回滚。

说话人 1 01:12:40
我们可以用 transactional 注解，在方法上加一个 transactional。我们再来看一下效果，我们先去把之前插入的这两条数据删一下，这个一和2是刚才插入的抛入异常以后并没有被删掉。现在我们确定这个表里边没有一和2了，我们再来看一下，我们发送这个时候它抛出了异常，在这个位置它抛出了一个除0的异常，我们看一下数据库里有没有刚才插入的这个两条一和2发现并没有插入，这就说明我们这个加的 transactional 注解起了作用。

说话人 1 01:13:46
在数据库进行插入了以后，其实不只是插入，包括更新，包括删除，你在中途出现了异常，它都会前面值进行的这些操作都会被回滚，会对数据库造成影响。所以我们在对数据库一个方法内对数据库进行多个更改的时候，这个 transactional 注解肯定是要加的，但是如果单纯的 select 的话，就没有必要加这个 transactional 注解了，因为 select 的话，你并没有对数据库更改，只是查出数据而已。所以，虽然这个 transactional 注解有用，但也不要滥用。