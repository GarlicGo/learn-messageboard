会议主题：1-后端培训项目 留言板 讲解（2）-480P 标清-AVC
上传时间：2026年3月31日（周二） 02:19 （GMT+08）
智能纪要由 AI 生成，可能不准确，请谨慎甄别后使用

总结
暂时无法在飞书文档外展示此内容
视频围绕留言板项目展开，涵盖需求分析、视图对象创建、接口开发、数据库设计及代码生成等内容，旨在推进项目的开发与实施，内容如下：
- 需求分析
- 用户类型与需求
  - 用户类型：留言板有普通用户和管理员两类用户。普通用户可发表、编辑、删除留言，举报留言或评论；管理员负责处理举报。
  - 具体需求：项目需满足用户注册与登录、查看与修改用户信息、查看与发表留言或评论、举报留言或评论以及管理员处理举报等需求。
- 用户与留言评论关系：一个用户可发表多条留言和评论，一个留言可拥有多条评论，但一个评论只能属于一个留言。用户与留言、用户与评论、留言与评论均为一对多关系。
[图片]
- 视图对象创建
  - 前端展示内容
    - 用户信息：前端展示的用户信息包含用户 ID、用户名、昵称和性别。
    - 留言信息：留言信息包括留言 ID、发布留言的用户昵称、留言类别、留言内容、留言包含的评论数、留言发布时间、留言最后一次修改时间和评论列表。
    - 评论信息：评论信息包含评论 ID、评论所属的留言 ID、评论楼层数、发布者的用户 ID、发布者的用户昵称、评论内容、评论发布时间、评论最后一次修改时间。
    - 举报信息：举报信息包含举报 ID、被举报的言论内容、举报原因和举报时间。
- 数据模型创建
  - 用户模型：在 API focus 中新建项目，在数据模型里新建分组 “视图对象”，创建用户模型，包含 user ID（int 型）、user name（string 型）、nickname（string 型）、sex（int 型，有固定取值）。
  - 留言模型：创建留言模型，包含留言 ID（message ID，int 型）、发布者 ID（user ID，int 型）、发布者昵称（nickname，string 型）、留言类别（category，int 型，对应类别见文档）、内容（content，string 型）、评论数（commentname，int 型）、创建时间（create time）、更新时间（update time）和评论列表（comment list，仅在留言详情页显示）。
  - 评论模型：创建评论模型，包含评论 ID（comment ID，int 型）、所属留言 ID（message ID，int 型）、楼层数（floor，int 型）、发布者 ID（user ID，integer 型）、发布者昵称（nickname，string 型）、评论内容（content，string 型）、创建时间（create time）和更新时间（update time）。创建完成后，在留言模型的评论列表中引用评论模型。
  - 举报模型：创建举报模型，包含举报 ID（int 型）、被举报的内容（string 型）、举报原因和举报时间。
[图片]
  - 返回模板创建
    - 通用返回模板：创建通用返回模板，格式为 access（布尔型，代表是否成功）、code（integer 型，业务状态码）、message（对业务状态码的解释）、data（object 型，数据，若无数据则为空）。
    - 分页返回模板：创建分页返回模板，用于需要分页的接口，包含总条数（total）、上页页码（prev，若无则为 -1）、下页页码（next，若无则为 -1）、数据列表（list，array 型，具体 object 结合接口确定）。
- 接口开发
  - 用户接口
    - 用户注册：接口路径为 /users/register，采用 post 方法，请求参数为包体参数，包含用户名和密码，返回响应引用通用返回模板，隐藏 data 字段。
    - 用户登录：接口路径为 /users/login，采用 post 方法，请求参数为包体参数，包含用户名和密码，返回响应引用通用返回模板，隐藏 data 字段。登录成功后，在 HTTP 响应的 header 中返回 TOKEN，前端需保存该 TOKEN。
    - 获取自己的信息：接口路径为 /users/info，采用 post 方法，请求头需携带 authorization 字段（即登录接口返回的 TOKEN），返回响应引用通用返回模板，data 字段为用户信息。
    - 获取指定用户的信息：接口路径为 /users/{userid}，采用 post 方法，请求头需携带 authorization 字段，返回响应引用通用返回模板，data 字段为用户信息。
    - 修改自己的信息：接口路径为 /users/info，采用 put 方法，请求头需携带 authorization 字段，请求参数为包体参数，包含用户昵称和性别，返回响应引用通用返回模板，隐藏 data 字段。
  - 留言接口
    - 发表留言：接口路径为 /messages，采用 post 方法，请求参数为包体参数，包含留言类别和留言内容，请求头需携带 authorization 字段和 request ID（防止重复提交），返回响应引用通用返回模板，隐藏 data 字段。
    - 编辑留言：接口路径为 /messages/{message ID}，采用 put 方法，请求头需携带 authorization 字段，请求参数为包体参数，包含留言类别和留言内容，返回响应引用通用返回模板，隐藏 data 字段。
    - 删除留言：接口路径为 /messages/{message ID}，采用 delete 方法，请求头需携带 authorization 字段，返回响应引用通用返回模板，隐藏 data 字段。
    - 查看留言列表：接口路径为 /messages，采用 get 方法，请求参数可在 query 参数中携带类别、用户 ID、分页参数（current 和 page size），请求头需携带 authorization 字段，返回响应引用分页返回模板，data 字段为留言列表。
    - 查看留言详情页：接口路径为 /messages/{message ID}，采用 get 方法，请求头需携带 authorization 字段，返回响应引用通用返回模板，data 字段为留言详情。
    - 举报留言：接口路径为 /messages/{message ID}/report，采用 post 方法，请求头需携带 authorization 字段，请求参数为包体参数，包含举报原因，返回响应引用通用返回模板，隐藏 data 字段。
  - 评论接口
    - 发表评论：接口路径为 /comments，采用 post 方法，请求头需携带 authorization 字段和 request ID（防止重复提交），请求参数为包体参数，包含所属留言 ID 和评论内容，返回响应引用通用返回模板，隐藏 data 字段。
    - 编辑评论：接口路径为 /comments/{comment ID}，采用 put 方法，请求头需携带 authorization 字段，请求参数为包体参数，包含评论内容，返回响应引用通用返回模板，隐藏 data 字段。
    - 删除评论：接口路径为 /comments/{comment ID}，采用 delete 方法，请求头需携带 authorization 字段，返回响应引用通用返回模板，隐藏 data 字段。
    - 举报评论：接口路径为 /comments/{comment ID}/report，采用 post 方法，请求头需携带 authorization 字段，请求参数为包体参数，包含举报原因，返回响应引用通用返回模板，隐藏 data 字段。
  - 管理员接口
    - 管理员登录：接口路径为 /admin/login，采用 post 方法，请求参数为包体参数，包含用户名和密码，返回响应引用通用返回模板，隐藏 data 字段。登录成功后，返回 TOKEN。
    - 查看待评审的举报列表：接口路径为 /admin/reports，采用 get 方法，请求参数可在 query 参数中携带分页参数（current 和 page size），请求头需携带 authorization 字段，返回响应引用分页返回模板，data 字段为举报列表。
    - 管理员评审举报：接口路径为 /admin/reports/review，采用 post 方法，请求头需携带 authorization 字段，请求参数为包体参数，包含举报 ID 和评审结果（illegal，布尔型），返回响应引用通用返回模板，隐藏 data 字段。
- 数据库设计
  - 逻辑设计
    - 关系处理：用户与留言、用户与评论、留言与评论均为一对多关系，将一方实体的主关键字处理为多方实体的外部关键字，即在留言表中添加用户表的主键 user ID，在评论表中添加用户表的主键 user ID 和留言表的主键 message ID。
    - 字段冗余：为提高查询速度，将留言的评论数冗余到留言表中，新增 comment num 字段。
    - 必备字段：每张表必须包含 ID、创建时间和更新时间三个字段。
  - 物理设计
    - 建表规约：表名和字段名全小写字母，用下划线分隔，表名不使用复数名词，禁用保留字；表达是与否的概念用 is_xxx 方式命名；类型为 decimal 且储存长度差、字符串长度几乎相等时用 char；定长串表必备 ID、create time、update time 三个字段。
    - 建表操作：在数据库中创建用户表（t_user）、留言表（t_message）、评论表（t_comment）和举报表（t_report），设置各表的字段、数据类型、主键和默认值等。
- Mybatis 代码生成
  - 插件安装：在设置的插件中搜索并安装 Mybatis x 插件。
  - 数据源配置：配置 idea 数据源，显示表信息。
  - 代码生成：选择要生成的表，如评论表，通过 Mybatis x Generator 进行代码生成，设置 model path、base package、relative package 等参数，忽略表前缀。生成 entity、Mapper、service 等相关代码，并对生成的代码进行处理，删除 Mybatis plus 相关的 extends 内容。
暂时无法在飞书文档外展示此内容

智能章节
00:01  留言板项目需求：用户功能与管理员处理
本章节主要围绕留言板需求文档展开，留言板有普通用户和管理员两类用户。普通用户可发表、编辑、删除及举报留言，管理员负责处理举报。项目需满足的需求包括用户注册与登录、查看与修改用户信息、查看及发表留言或评论、编辑或删除留言或评论，以及举报留言或评论和管理员处理举报。
00:43  用户、留言与评论的关系阐述
本章节主要探讨了用户留言与评论之间的关系。一个用户能发表多条留言和评论，即用户与留言、用户与评论均为一对多关系；一条留言可有多条评论，但一条评论只属于一个留言，所以留言与评论也是一对多关系。
01:18  前端展示视图对象创建及接口文档完善
本章节主要讨论前端展示内容及相关数据模型创建。确定前端展示的用户、留言、评论、举报应包含的信息，在 API focus 新建项目，于数据模型中新建分组“视图对象”并创建用户、留言、评论、举报 4 个数据模型，还创建了通用和分页返回模板，最后完善接口文档，将常量写入并保存。
14:39  接口文档编写及数据库设计概念讲解
本章节主要围绕接口创建展开，先完成用户相关5个接口，遵循restful风格；接着创建留言相关6个接口、评论相关4个接口以及管理员相关3个接口，涵盖参数设置、返回响应等。最后讲解数据库设计，提及概念设计中用户与留言、评论，留言与评论的一对多关系。
56:36  逻辑设计要求及用户、留言、评论表字段添加
本章节介绍了逻辑设计的要求，主要涉及一对多和多对多关系。项目常用一对多，即把一方实体主关键字处理为多方实体外部关键字，如用户与留言、评论的关系；还提到可将留言评论数冗余到留言表加快速度。此外，要求每张表都有 ID、创建时间和更新时间，最后说明了各表新增的字段。
01:00:57  数据库物理设计：建表规约与多表创建演示
本章节主要围绕数据库物理设计展开。先回顾举报表字段设置，介绍建表规约。接着演示在数据库中创建多个表，包括用户表、留言表、评论表和举报表，详细说明各表字段的数据类型、是否为空、默认值等，最后还提及如何查看表的 DDL 定义及建表语句。
01:23:41  Mybatis代码生成器使用方法及代码处理
本章节介绍了Mybatis代码生成器的使用。先在设置插件里搜索安装Mybatis x插件，配置idea数据源。选表后通过Mybatis x Generator生成代码，设置包名、路径等参数，忽略表前缀。完成一张表后，其余表利用记忆功能操作。最后对生成代码中Mybatis plus相关的extends等进行删除处理，代码生成器生成多类文件，节省编写时间。
暂时无法在飞书文档外展示此内容

关键决策
- 关键决策：完成留言板项目的接口文档编写和数据库设计，并使用 MyBatis 代码生成器生成相关代码。
  - 问题：设计留言板项目的系统架构，包括接口文档、数据库表结构和代码生成。
  - 讨论方案：
    - 说话人 1 详细阐述了留言板的用户需求，包括普通用户和管理员的操作，以及用户与留言、评论之间的关系。
    - 确定了给前端展示的视图对象，包括用户、留言、评论和举报，并创建了相应的数据模型。
    - 按照 RESTful 风格编写了用户、留言、评论和管理员相关的接口文档。
    - 进行数据库设计，包括概念设计、逻辑设计和物理设计，确定了表结构和字段。
    - 使用 MyBatis 代码生成器生成实体类、Mapper 接口、Service 接口和实现类，以及 XML 文件。
  - 决策依据：根据项目需求和设计原则，逐步完成系统架构的设计和实现，提高开发效率和代码质量。

---
- 其他决策：
  1. 数据库表名采用 t_ 下划线开头，全小写字母，用下划线分隔，不使用复数名词，禁用保留字。
  2. 表必备 ID、create_time、update_time 三个字段。
  3. 表达是与否的概念用 is_ 下划线加名称的方式命名字段。
  4. 若类型为 decimal 且储存长度差、字符串长度几乎相等，使用 char 类型。
  5. 忽略 MyBatis 代码生成器生成代码中 MyBatis Plus 相关的 extends 部分。
暂时无法在飞书文档外展示此内容

金句时刻
「RESTful 是一种设计风格，其 URL 里使用名词，用 HTTP 方法表示操作，而非将动词写在 URL 里。」
—— 说话人 1，清晰地解释了 RESTful 设计风格的核心要点，有助于团队成员理解和遵循该风格进行接口设计。

「数据库设计中，常用的关系处理方式有一对多和多对多。一对多是将一方实体的主关键字处理为多方实体的外部关键字；多对多是创建一个新表，包含两个关联表的主键。」
—— 说话人 1，总结了数据库设计中常见关系的处理方法，为数据库设计提供了重要的指导原则。

「MyBatis 代码生成器帮我们生成了很多东西，包括实体类、Mapper 层的 Java 接口、Service 接口与实现类，以及 XML 文件，这样我们就不用再自己写一遍了，提高了开发效率。」
—— 说话人 1，强调了 MyBatis 代码生成器的作用，体现了其在项目开发中节省时间和精力的价值。
暂时无法在飞书文档外展示此内容
